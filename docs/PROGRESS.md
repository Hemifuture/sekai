# Sekai 项目进度

更新时间：2026-08-03

当前主线里程碑：自然世界底座

合并节点：`13b8118`（`feature/causal-island-relief` → `main`）

## 当前结论

Sekai 第一阶段仍然聚焦前工业、中世纪幻想世界的当前时间切片，不实现历史时间线。当前版本已经把世界基础、可观察显示和自然底座的主链贯通到：

```text
空间拓扑
  → 世界形成预设
  → 板块 / 地壳 / 当前边界运动
  → 地幔热点 / Relief / 地质
  → 初步气候
  → 水文 / 侵蚀
  → 当前地表与只读字段显示
```

这是一条可运行、可验证的自然生成纵切，不等于第一阶段全部完成。魔法、生态、社会、作者工具、存储和外部城镇生成器链接仍在后续范围内。

## 已交付里程碑

| 里程碑 | 状态 | 结果 |
|---|---|---|
| 世界基础与生成引擎 | 已完成当前所需底座 | 类型化 ID、单位、空间快照、阶段图、确定性随机、缓存、诊断与验证 |
| 独立字段显示 | 已完成 V1 | 只读字段目录、中文标签、标量/分类调色、检查器和 GPU 字段渲染 |
| 规则能力与作者约束 | 已完成自然链所需底座 | 显式规则解析、能力边界、约束投影和确定性输入 |
| 构造自然底座 | 已完成 | 板块速度、相邻板块相对运动、边界段与地壳字段 |
| 地质基底 | 已完成 V1 | 地幔热点、Relief V3、岩性、裂隙与形成潜势 |
| 初步气候 | 已完成基础切片 | 温度、降水、季节性与海洋性影响 |
| 水文与侵蚀 | 已完成基础切片 | 汇流、地表水、河流级序、侵蚀、沉积和当前地表 |
| 世界形成预设 | 已完成 V1 | 随机、多大陆、群岛、超级大陆、大岛与卫星岛、火山群岛；预设与板块系统保持正交 |
| 自然地图可信度修正 | 已完成 | 自然海洋走廊、连续海底起伏、构造响应和封闭海洋边框 |
| 因果岛屿与多尺度地貌 | 已完成并合入 `main` | 热点岛组、海山、洋—洋俯冲岛弧、分辨率受限噪声与因果约束 |

各里程碑的详细契约和任务记录位于 [`superpowers/specs`](./superpowers/specs/) 与 [`superpowers/plans`](./superpowers/plans/)。

## 本次自然底座检查点

本次合并解决了海洋过于干净、只有大块陆地而缺少有成因小岛的问题，同时保持原有系统边界：

- 热点仍由 Mantle 唯一拥有；Relief 只在其正式正影响域内塑造海山与火山岛组。
- 洋—洋俯冲岛弧只读取 Tectonics 已分类的当前边界，并写入 `tectonic_offset_m`。
- `volcanic_offset_m` 只表达热点火山地貌；噪声不能脱离因果支撑独立造陆。
- 世界形成预设不改变板块 ID、归属、速度或边界分类，只把宏观初态传给后续阶段。
- 外边界与东西可见边框继续保持海洋；当前有限平面没有伪装成已经实现的无缝球面。
- 生成只解释当前切片，不新增年龄、事件、轨迹或时间轴。

## 发布级验证记录

功能分支在合并前完成了以下验证：

- `cargo fmt --all -- --check`、`git diff --check`、all-feature Check 和严格 Clippy 通过。
- `cargo test --all-targets --all-features` 通过：191 个库测试通过，1 个既有压力测试保持忽略；集成、可执行、基准和 GPU 参考目标通过。
- release golden 与 8 个固定种子 × 5 个具体预设的形态矩阵通过人工审阅。
- 火山群岛的固定种子均保留 6–18 个可归因的洋壳岛屿分量；`0x00C0_FFEE` 为 11 个。
- 所有固定质量种子的外边界及东西边框陆地单元数均为 0。
- 20,000 单元 release 性能门总耗时 `1794.625 ms`；其中 Mantle `12.805 ms`、Relief `38.459 ms`。
- wasm32 all-feature 检查和 `trunk build` 通过。
- release 桌面应用已检查当前地表、海陆、地壳、构造偏移、火山影响和火山偏移视图；岛屿呈稀疏不规则组团或断续弧，而非全局椒盐噪声、等距珠串或圆形印章。

上述数据是 2026-08-03 自然底座检查点的回归基线，不是对未来提交永久不变的性能承诺。

## 主线发布验证

`13b8118` 合并后的 `main` 在推送前重新通过：

- README/进度页本地链接检查、`cargo fmt --all -- --check` 与 `git diff --check`；
- all-feature Check 与 `cargo clippy --all-targets --all-features -- -D warnings`；
- `cargo test --all-targets --all-features`：191 个库测试通过、1 个既有压力测试忽略，其他测试目标通过；
- release golden 和固定种子质量矩阵：2 个测试通过、1 个写入型 golden 更新测试按设计忽略；
- 20,000 单元 release 性能门：总耗时 `1801.849 ms`，Mantle `13.362 ms`，Relief `41.410 ms`；
- wasm32 all-feature library Check 与 `trunk build`。

本机 Windows 没有被 wgpu 标记为 fallback 的软件 GPU 适配器，因此设置 `SEKAI_REQUIRE_FIELD_GPU=1` 的 CI 专用强制模式会明确报告 `no fallback adapter is available`；普通本地测试按契约跳过该可选证据。该测试和请求 fallback 的逻辑自 `6f198b1` 起未被本次合并修改。强制 GPU/CPU 离屏一致性由安装 Mesa Vulkan 的 `field_display_gpu` CI job 提供，不以本机硬件渲染器替代软件参考证据。

## 尚未交付

- 完整土壤、生态、生物群系和自然资源闭环；
- 魔力源汇、地脉、异常区、传送连接及其自然作用；
- 非人智慧物种、人口、文化、聚落、路线、贸易、政体与当前边界；
- 作者编辑命令、撤销重做、项目保存、迁移和导出；
- 聚落到外部城市/村庄布局生成器的确定性链接；
- 历史年份、编年史、事件生成、演化和回放。

其中最后一项明确不属于当前第一阶段。未来若启动历史系统，必须独立设计，不能把时间状态反向渗入已经稳定的当前切片契约。

## 后续方向

下一步应继续沿当前切片的依赖方向补齐自然世界，而不是启动历史模拟。进入新里程碑前，应从总体设计中选择一个边界清晰的专项切片，先确认其输入、权威输出和验收标准；优先候选是土壤—生态基础，或对当前气候/水文链的质量深化。具体选择尚未在本检查点承诺。
