# P4 水热校正设计（里程碑 A4）

日期：2026-09-03
状态：**冻结**（用户 2026-09-02 指示"开始做 P4 水热校正里程碑"）

上位：`AGENTS.md`；P4 设计 `2026-08-17-global-atmosphere-ocean-p4-design.md`
§5、§7；A2 `2026-09-02-p4-zonal-asymmetry-design.md` §6；A3
`2026-09-02-p4-land-evapotranspiration-design.md` §5.1。
实施计划：`../plans/2026-09-03-p4-water-heat-correction.md`。

## 1. 病征（Task 0 实测，Draft seed 42，5 轮，残差 0.104）

探针 `tests/p4_zonal_profile_probe.rs` 按 5° 纬带做面积加权年平均。`T_eq` 是
年平均吸收短波的灰体平衡温度减地形直减；`ML0` / `air0` 按 `state.rs`
`role_reference_temperature_c` 从发布的逐月吸收短波重建初值。

| 纬带 | T_air | T_eq | SST（发布） | ML0（重建） | air0（重建） | P mm/day | RH % | TOA W/m² | Tjul−Tjan |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 87.5 N（全海） | −19.8 | −20.6 | **10.0** | 9.9 | **−37.7** | 0.00 | 36 | +12.7 | 2.1 |
| 62.5 N | −18.1 | −6.5 | 11.0 | 11.0 | −21.1 | 0.01 | 72 | +13.0 | 1.8 |
| 47.5 N | **−7.2** | 9.9 | 15.0 | 15.0 | 3.6 | 3.99 | 96 | +14.6 | 0.0 |
| 2.5 N | 19.9 | 23.2 | 23.4 | 23.2 | 23.2 | 1.83 | 67 | +3.4 | 0.1 |
| −47.5 | −12.3 | 4.2 | 12.3 | 12.2 | −2.0 | 3.56 | 92 | +27.5 | −1.4 |
| −87.5（全陆） | −18.0 | −37.3 | — | 4.8 | −46.8 | 0.00 | 6 | +13.3 | 0.8 |
| 全球 | 10.3 | 13.2 | — | — | — | 2.40 | 68 | +7.0 | — |

地球参考：极地 2 m 气温 −20 ~ −35 °C，45° 带 8 °C、降水 2.6 mm/day，赤道 4.6
mm/day，大陆 45° 带 Tjul−Tjan 20–30 K。

1. **发布海温 = 初值**：36 个纬带 SST 与 ML0 逐带吻合到 0.1 K，季节振幅全为 0。
2. **初值偏离年平均平衡**：北极混合层初值 +10 °C（平衡 −21），大气初值 −38 °C；
   南极大气 −47 °C（平衡 −37）。
3. **45–55° 冷池**：气温比平衡冷 17–20 K；这是极区 −40 °C 初值空气被 3 m/s 的
   低层经向流在 5 天内推到 50° 的瞬变，不是任何平衡态。
4. **降水在 40–50° 倾倒、60° 以外为零、热带无 ITCZ**：冷池令空气饱和（RH 96 %），
   极区空气反被 +10 °C 海面加热而干燥（RH 36 %）。
5. **TOA 45° 以外 +13 ~ +28 W/m²**，季节振幅 ≤ 2 K。

## 2. 成因（代码事实）

1. **目标温度是逐月瞬时辐射平衡。** `forcing.rs` 对每月吸收短波取
   `gray_equilibrium_surface_temperature_c`，无任何热惯性；极夜月 ASR = 0 给出
   −239 °C，被钳到 −90 °C。混合层初值 = 逐月值钳到 [−2, 40] 后取年均：极夜被
   钳掉、极昼留下 +10 °C，均值偏暖 30 K；大气初值 = 钳到 −90 °C 的月均，偏冷 17 K。
   这一套逐月目标同时是：状态初值（`state.rs`）、辐射线性化中心
   （`apply_external_radiation`）、层间热交换的距平参考
   （`equilibrium_anomaly_heat_exchange`：交换松弛的是 `T − T_ref`）、湿度目标与
   轴对称环流诊断的输入。骨架错，全身错。
2. **OLR 线性化围绕瞬时目标。** `linearized_outgoing_longwave_w_m2` 的截距是当月
   ASR、斜率 `4σT_e³` 取当月目标的发射温度：极夜时斜率只剩 0.75 W/m²/K（正常
   3.6），−20 °C 空气的 OLR 被算成 53 W/m²（灰体真值 130）。
3. **6 天成形 ≪ 热力学时标。** 低层大气热容 ρc_pH = 1.225 × 1004 × 6000 =
   7.38 × 10⁶ J/m²/K，辐射时标 C/B = 24 天；混合层 1025 × 3990 × 100 = 4.09 × 10⁸
   J/m²/K，时标 3.6 年。5 轮 × 1 天里大气只消化掉初值误差的 19 %，混合层 0 %。
   发布的就是"错误初值 + 瞬变"，而非任何平衡态；季节循环无从产生。
4. **无海冰。** 混合层被钳在 −2 °C 却仍以开阔水面参与辐射权重与蒸发，极区空气
   由 −2 °C（乃至 +10 °C）的"海面"托住。

成因 1–2 与 4 在本里程碑修；成因 3 是时间结构问题，§6 给出选项交用户裁定。

## 3. Task 1 —— 储热一致的季节目标与年平均线性化

### 3.1 逐月目标：线性能量平衡的周期解

每格每个受辐射的角色用一维储热方程（Budyko 1969；North & Coakley 1979）：

```text
C dT/dt = ASR(t) − [A + B T]
A + B T ≡ ASR_ann + B (T − T_eq,ann)
T_eq,ann = gray_equilibrium(ASR_ann) + offset − Γ_oro · z      （年平均目标，含地形直减）
B        = 4 σ (T_eq,ann + 273.15 − ΔT_gh)³                     （在年平均发射温度处取斜率）
```

ASR 逐月分段常数，方程逐月精确积分 `T_{m+1} = T_eq,m + (T_m − T_eq,m) e^{−BΔt/C}`，
`T_eq,m = T_eq,ann + (ASR_m − ASR_ann)/B`，取 12 个月的周期不动点（闭式，无迭代）。
单谐波极限即 North & Coakley 的振幅 `ΔF/√(B² + ω²C²)`、滞后 `atan(ωC/B)`。

角色热容：
- 混合层目标：`C = C_ml`（布局 100 m）。45° 处 ΔF ≈ 150 W/m² → 振幅 1.8 K、滞后
  2.9 个月（地球中纬 SST 3–6 K、2 个月；100 m 比季节混合层偏深，沿用布局 SSOT）。
- 低层大气目标：`C = C_air + (1 − land) · C_ml`——空气加其下储热的水柱。陆上
  振幅 ≈ ΔF/3.9 ≈ 38 K、滞后 0.7 月（无输运的局地上限）；海上随海温。
- 极夜下限有限：`T_eq,m(ASR = 0) = T_eq,ann − ASR_ann/B`，北极 ≈ −56 °C，再经
  储热衰减，不再出现 −239 °C 与 −90 °C 钳制。

混合层 / 温跃层 / 深海目标继续沿用现有钳制（−2 / −5 / 40 °C）与偏移；湿度目标
`RH_ref · q_sat(T_air,m)` 公式不变。逐月目标仍是逐月字段，`seasonal-hemisphere-*`
度量与轴对称诊断按新目标工作。

### 3.2 初值 = 年平均目标

`state.rs` 年平均初值改取 `T_eq,ann`（各角色再按现有钳制 / 偏移），不再对逐月
钳制值取均值。逐月目标的年均与 `T_eq,ann` 在线性方程下恒等，二者一致。

### 3.3 OLR 围绕年平均线性化

`apply_external_radiation` 改为 `OLR = ASR_ann + B_ann (T_s − T_eq,ann)`（经典
`A + B T`），`requested_power = ASR_m − OLR`。季节 TOA 不再被强迫为零（储热是
物理），年平均 TOA = 0 当且仅当 `T_ann = T_eq,ann`。高地直减仍进 `T_eq,ann`，
不制造虚假 TOA 源（原设计意图保留）。

### 3.4 指纹与契约

强迫指纹 v4 → v5；方程指纹 v11 → v12；P4 设计 §5 / §7 加修订段。年平均量
（`ASR_ann`、`T_eq,ann`、`B_ann`）不新增字段：储热一致目标的 12 个月均值恒等于
年平均目标，辐射步直接对逐月字段取均值，强迫契约形状不变。

### 3.5 实测（Task 1，Draft seed 42）

| 量 | Task 0 | Task 1 | 参考 |
| --- | ---: | ---: | --- |
| 北极 SST | +10.0 | **−1.9** | 钳制 −2（无海冰） |
| 北极气温（T_eq −20.6） | −19.8 | −8.9 | 地球 −20 |
| 47.5° N 气温（T_eq 9.9） | −7.2 | **3.7** | 地球 8 |
| 45–90° N TOA W/m² | +12 ~ +15 | **−0.3 ~ +0.3** | ≈ 0 |
| 南极气温（T_eq −37.3） | −18.0 | −9.9 | 地球 −35 |
| 南极 TOA W/m² | +13.3 | −49.0 | ≈ 0 |
| 全球气温 / TOA | 10.3 / +7.0 | 14.4 / −3.8 | 13.2（T_eq）/ 0 |
| 全球 P / E mm/day | 2.40 / 2.51 | 2.43 / 2.55 | 2.81 |

海洋主导的北半球按预期收敛：海温落到目标、高纬 TOA 归零、冷池从 −17 K 缩到
−6 K。陆地主导的南极暴露了成因 3：初值已是 −37 °C 的年平均目标，5 天里平流
输入约 60 W/m² 的热（经向温差 60 K、3 m/s），辐射响应（时标 24 天）来不及
平衡，气温升到 −10 °C、OLR 比 ASR 高 49 W/m²。收敛态应停在
`B (T − T_eq) = 平流加热` ≈ +17 K，即 −20 °C（地球南极沿岸量级）。40–50° 的
降水倾倒与极区无降水仍在——它们随温度场的瞬变一起留给 §6。

## 4. Task 2 —— 海冰面先验

北 1975 冰线：年平均（无冰）表面目标 `T_eq,ann,surface < LIQUID_MIXED_LAYER_MIN_C`
（−2 °C）的海面视为冰封，冰封分数 `ice ∈ {0, 1}` 一次诊断、不迭代（与高地雪
反照率斜坡同为强迫先验，避免多重平衡）。冰封海面：

1. 反照率 `P4_SEA_ICE_SURFACE_ALBEDO = 0.60`（Perovich et al. 2002：雪覆盖冰
   0.80–0.85、融池期 0.40–0.50，取年包络中值）；ASR 与 `T_eq,ann` 随之重算。
2. 辐射权重归低层大气（同陆地）：`weights = [1 − w, w]`，`w = (1 − land)(1 − ice)`。
3. 蒸发可用度 `= (1 − land)(1 − ice)`（升华 ≈ 0.1 mm/day，忽略）。
4. 气–海热交换的 `water_scale` 乘冰层传导比 `1/13`：海冰 k = 2.03 W/m/K
   （Untersteiner 1961）/ 平衡厚度 3 m（Maykut & Untersteiner 1971）= 0.68
   W/m²/K；开阔水面 `ρ c_p C_H U` = 1230 × 1.2 × 10⁻³ × 6 m/s = 8.9 W/m²/K
   （Large & Pond 1982 的 C_H；6 m/s 为海面平均风）。
5. 冰下混合层目标仍为 −2 °C（现有钳制）。

冰线判据只看海面：部分陆地格的海面在海平面，判据不带地形直减（实施时测试
抓到 38° S 的高海拔半陆格被判成冰面，已修）。

### 4.1 实测（Task 2，Draft seed 42；代码在分支 `a4-task2-sea-ice-experiment`）

| 量 | Task 1 | Task 2 | 参考 |
| --- | ---: | ---: | --- |
| 北极气温 | −8.9 | **−32.8** | 地球 −20 |
| 北极 T_eq（含冰反照率） | −20.6 | −62.6 | 无输送平衡 |
| 冰线（北半球海洋） | — | ≈ 60° N | 地球 68–70° N |
| 52.5° N 气温（T_eq 4.9） | −3.1 | **−22.8** | 地球 1 |
| 60–90° N TOA W/m² | ≈ 0 | −29 ~ −37 | 地球 −100（由输送补） |
| 全球 T / T_eq | 14.4 / 13.2 | 10.8 / 8.4 | — |
| 全球 TOA | −3.8 | −9.0（夹具 −12.7，破 ±10 门） | 门 ±10 |

海冰让极区物理变对（冰面不再蒸发、辐射直接作用于空气、极区空气降到 −33 °C），
但冰线按"无输送平衡 < −2 °C"落在 60°（地球 SST 在 60° N 约 5 °C 全靠输送），
冰盖初值 −60 °C，5 天瞬变把 45–60° 推成 −28 K 的冷带，生成夹具破 TOA 硬门。

### 4.2 实验 Task 2b —— 扩散 EBM 年平均态作冰线判据与初值

North (1975) 一维 EBM 把经向输送写成扩散；在工作网格上解其二维形式（对称正定，
共轭梯度）`B_i (T_i − T_eq,i) = (κ/A_i) Σ_j (L_ij/d_ij)(T_j − T_i)`，
`κ = D R²`，`D = 0.649 W m⁻² K⁻¹`（North, Cahalan & Coakley 1981）。无冰解定冰线，
冰反照率下的最终解作各角色初值；辐射线性化与交换距平参考保持无输送形式以免
与分辨的平流重复计数。实测（seed 42）：

| 量 | Task 1 | Task 2b（D 按 D/B 比缩放 = 1.12） | Task 2b（D = 0.649） | 地球 |
| --- | ---: | ---: | ---: | ---: |
| 冰线 | — | 无冰（极区海面 +6 °C） | 无冰（极区海面 +2 °C） | 68–70° |
| 北极气温 | −8.9 | −0.9 | −3.2 | −20 |
| 赤道气温 | 21.1 | 16.9 | 17.8 | 26 |
| 全球 P mm/day | 2.43 | 1.98 | 1.92 | 2.81 |
| 全球 TOA | −3.8 | +3.2 | +3.3 | 0 |

### 4.3 结构性结论：灰体辐射先验已隐含地球的输送

无冰扩散解在极区高于冰点、在赤道比 T_eq 冷 8 K，说明 P4 的无输送剖面
（赤道 26、极地 −21，反差 47 K）本身就接近地球**含输送**的实际剖面（26 / −20）。
原因在辐射先验：大气反射率 `EARTH_ATMOSPHERIC_SHORTWAVE_REFLECTANCE` 与温室偏移
`EARTH_GRAY_GREENHOUSE_OFFSET_K` 都是按 CERES 全球均值标定的**常数**，没有纬度
结构——地球无冰时极区行星反照率约 0.52（云与低太阳高度角，NCC 1981 的
`a(x) = 0.681 − 0.202 P₂(x)`）而本模型为 0.34，热带水汽温室 45 K 而极区 25 K，
本模型统一 34 K。于是"无输送平衡"已经偏暖于真实极区、偏冷于真实热带，恰好抵消
了输送；任何显式经向输送（扩散 EBM，或时间结构改成能收敛的分辨动力学）都会
把它再抹平一次。**海冰先验与含输送初值都不能在这个先验之上单独成立**；它们
依赖一个带纬度结构的辐射先验（行星反照率 `a(φ)`、水汽温室随温度），而那是
对 P4 17 seed 校准语料的重标定，属架构裁定。Task 2 / 2b 因此不进 main，代码与
测试保留在分支 `a4-task2-sea-ice-experiment`（提交 ba5ebdf）。

## 5. 验证

- seed 42 纬带探针（Task 0）作为前后证据：极区 SST、`air0`、45° 冷池、极区 RH 与
  降水、高纬 TOA；
- 17 seed 语料：全球 P、TOA、雨影 / 增湿、非纬向方差、季节相位度量；
- 32 seed 冷启动扫描、全量 Release 回归、产品级时延门。

### 5.1 实测（Task 3，main = Task 1 态）

| 指标（17 seed Draft） | A3 | A4 Task 1 | 参考 |
| --- | ---: | ---: | --- |
| 全球降水 mm/day | 2.66 | 2.66（2.28–2.87） | GPCP 2.81（±7 %） |
| 全球蒸发 mm/day | 2.76 | 2.77 | ≈ 2.8 |
| TOA W/m² | +7.2 | **−5.0**（−6.6 ~ −3.0） | 0.9，门 ±10 |
| 雨影度量失败 | 4/17 | 5/17 | A0 1/17 |
| 增湿比失败 | 7/17 | 6/17 | A0 6/17 |
| 海流陆地泄漏失败 | 17/17 | 17/17 | 既有问题 |
| `seasonal-hemisphere-phase-fraction` 失败 | 0/17 | **17/17** | 门 ≥ 0.65（软） |

全量 Release 回归 151 个结果全绿；32 seed 冷启动 Draft 32/32、Standard 32/32；
时延 Draft 20.1 s、Standard 64.7 s（与 A3 持平，Task 1 无可测开销；Standard 仍
超 60 s 目标，A1 遗留）。

季节相位度量的翻转是 Task 1 的直接后果，不是回归：它检查 7 月 − 1 月气温在各
半球的符号。此前逐月瞬时目标把这个符号通过交换距平参考硬灌进 ≤ 2 K 的季节
信号；储热一致目标在海上滞后 2.9 个月、振幅 1–2 K，5 天瞬变里符号随机。它证明
的是成因 3（模型不分辨季节循环），度量本身保留，等时间结构裁定。

## 6. 时间结构（交用户裁定）

Task 1–2 修正的是目标与初值；成因 3 意味着无论目标多正确，5 天积分都只能发布
"初值附近的瞬变"：陆上气温只走完与目标差距的 19 %，海温不动，季节循环 ≤ 2 K。
三条路：

| 选项 | 内容 | 代价 | 后果 |
| --- | --- | --- | --- |
| A 诊断式热力学 | 热力学（辐射、交换、相变）与温湿输运按"月时钟"（每宏步 = 1 个月）隐式积分（后向 Euler 迎风，M 矩阵，Gauss–Seidel），动力学仍按 2 h 显式 | 重：`tendency.rs` 热力 / 湿度路径重写、账本与契约重钉、度量重校 | 发布态 = 季节准平衡；风场真实输送热与水；季节循环真实 |
| B 局地加速 | 仅局地热力学按 Bryan (1984) 失真物理加速，输运不动 | 中 | 输运相对削弱 365 倍 → 温度退化为局地平衡 + 交换：极地 −37 °C、无经向输送；与 P4 "分辨环流"目标矛盾 |
| C 维持 | 保留 6 天瞬变 | 零 | 发布态为瞬变、无季节循环；A4 只能修目标与初值 |

§4.3 把裁定加了一层：**时间结构之前先要辐射先验的纬度结构**。A 让分辨动力学
真正输送热量，那么先验必须退回"无输送"的本来面目（极区更冷、热带更暖），否则
A 的收敛态会是被抹平的世界；B 与 C 则继续依赖先验隐含的输送。建议顺序：

1. 里程碑"P4 辐射先验"：行星反照率随纬度（NCC 1981 `a(x)` 或天顶角 + 云带
   先验）、温室偏移随水汽（Manabe & Wetherald 1967 类的相对湿度固定假设），
   以 17 seed 语料重标定全球均值门；
2. 里程碑"P4 时间结构"选项 A；
3. 之后海冰先验（§4）与含输送初值（§4.2）自然成立。

本里程碑交付 Task 0–1 与 Task 3 的证据，Task 2 / 2b 作为实验分支留给上述裁定。

## 7. 出处

| 项 | 出处 |
| --- | --- |
| 线性 OLR `A + B T`、零维 / 一维储热 EBM | Budyko, M. I. (1969) *Tellus* 21, 611–619；North, G. R. (1975) *J. Atmos. Sci.* 32, 2033–2043 |
| 季节 EBM 的振幅 / 滞后 | North, G. R. & Coakley, J. A. (1979) *J. Atmos. Sci.* 36, 1189–1204 |
| 冰线 | North (1975) 同上 |
| 海冰反照率季节包络 | Perovich, D. K., Grenfell, T. C., Light, B. & Hobbs, P. V. (2002) *JGR* 107, 8044 |
| 海冰热导 / 平衡厚度 | Untersteiner, N. (1961) *Arch. Met. Geophys. Bioklim. A* 12, 151–182；Maykut, G. A. & Untersteiner, N. (1971) *JGR* 76, 1550–1575 |
| 海面感热体块系数 | Large, W. G. & Pond, S. (1982) *J. Phys. Oceanogr.* 12, 464–482 |
| 失真物理加速（选项 B 的出处） | Bryan, K. (1984) *J. Phys. Oceanogr.* 14, 666–673 |
| EBM 扩散系数 `D = 0.649`、无冰共反照率 `a(x) = 0.681 − 0.202 P₂(x)` | North, G. R., Cahalan, R. F. & Coakley, J. A. (1981) *Rev. Geophys. Space Phys.* 19, 91–121 |
| 固定相对湿度下的水汽温室 | Manabe, S. & Wetherald, R. T. (1967) *J. Atmos. Sci.* 24, 241–259 |
