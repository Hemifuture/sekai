# P4 水循环量级与热带雨带设计（里程碑 A5）

日期：2026-09-03
状态：**Task 0 已实测，设计随测量修订中**

上位：`AGENTS.md`；A4 `2026-09-03-p4-water-heat-correction-design.md` §6.5、§6.6。
实施计划：`../plans/2026-09-03-p4-water-cycle-and-tropics.md`。

A4 收尾时给用户排的三件事是：一、蒸发用错了风（判为最高优先，因为它可能连带
解开压缩比上限）；二、赤道辐合带；三、辐射先验的纬度结构。用户批准全做。
Task 0 的实测**推翻了第一件的前提**，本设计据实测重排。

## 1. Task 0 实测（2026-09-03）

探针加了蒸发闭合的分解打印（`[evap]` 行），语料写入器加了每个种子的陆地占比。

### 1.1 风偏弱，不是偏强

| 量 | 模型 | 地球 |
| --- | ---: | ---: |
| 海面平均风速（探针，因果成形管线） | 5.11 m/s | 6.6 m/s（10 m 洋面风） |
| 海面平均风速（语料，17 seed 均值） | 5.76 m/s | 同上 |
| 海气比湿差（探针） | 5.5 g/kg | — |
| 海面蒸发（探针） | 3.42 mm/day | ≈ 3.4 mm/day |

原判断是"蒸发用的是 6 km 板平均风，比近地面风强，所以偏湿"。实测相反：发布的
`near_surface_wind_m_s` 就是低层板速度本身，其海面均值 **低于** 地球的 10 m 洋面
风。偏大的是饱和差。两个偏差方向相反、量级相近，乘起来给出接近地球的海面蒸发
——**又一个补偿误差**，与 A4 反复遇到的同类。

因此"改用近地面风"这条单点修改是错的：它会把偏弱的风再调弱，让蒸发塌掉。

### 1.2 湿偏差不是海陆比造成的

| 量（17 seed） | 值 |
| --- | ---: |
| 陆地占比均值 | 0.286（地球 0.29） |
| 陆地占比范围 | 0.273 – 0.305 |
| `corr(陆地占比, 降水)` | +0.16 |
| 全球降水均值 | 3.009 mm/day（GPCP 2.81，+7 %） |

海陆比与地球一致且与降水几乎不相关，所以 +7 % 是真实偏差，不是"不同星球的海陆
比不同"。但它仍在 ±7 % 包络内，潜热 93 W/m² 也在 Stephens et al. (2012) 的
78–98 内（高出 Wild 的 70–85）。

### 1.3 语料与探针是两条不同的管线

同一个 seed 42：

| 管线 | 陆地占比 | 全球降水 |
| --- | ---: | ---: |
| 探针（`causal_formation_fixture`，完整因果成形） | 0.256 | 2.852 |
| 语料写入器（初级地形 + 气候，无因果成形） | 0.305 | 3.112 |

地形不同，气候自然不同。**A4 的记述里把两组数当同一件事比较过**，例如"seed 42
降水 2.85 而语料 3.01"被读作种子间离散，实际上其中还混着管线差异。今后引用
必须写明管线。

## 2. 结论与重排

1. **第一件（蒸发的风）取消**，前提被实测推翻。真正的成因是"单层湿度既要当
   近地面比湿供体块蒸发用、又要当柱平均湿度供凝结用"，这两个角色互相矛盾。
   修它就是给湿度加垂直结构，而那正是第二件的前置。于是**第一件并入第二件**。
2. **第二件（赤道辐合带）** 的两个前置在 A4 §6.5 已经钉死：缺经向翻转环流；
   湿度无垂直结构。后者同时是 §1.1 那对补偿误差的根源。
3. **第三件（辐射先验的纬度结构）** 不变，代价是 17 seed 语料重标定。

湿偏差 +7 % 落在包络内，且它的两个成因彼此抵消，单独动任何一个都会变差。
因此本里程碑**不单独修湿偏差**，它会随湿度垂直结构一起解决或一起暴露。

## 3. Task 1 —— 水汽柱质量与凝结阈值（2026-09-03）

### 3.1 症结：比湿是近地面值，却按干空气柱质量换算成水量

模型低层大气的比湿是一个**近地面值**：体块蒸发把它当作与海面交换的空气，凝结
阈值拿它与同一温度下的饱和值比，发布的气温也是拿去和地球 2 m 场对照的。但把
它换算成水量时用的是这一层的**干空气柱质量** `ρ × 6000 m = 7350 kg/m²`。水汽
不像干空气那样分布：在均匀相对湿度假设下（Manabe & Wetherald 1967）比湿随
`q_sat(T(z))` 衰减，标准直减率把它集中在最低约两公里。

实测（探针，Draft seed 42）：

| 量 | 修前 | 地球 |
| --- | ---: | ---: |
| 可降水量 | 65.6 kg/m² | 25 |
| 水汽滞留时间 | 23.0 天 | 8–9 |

两者**同为 2.6 倍**，正是干空气柱质量与水汽柱质量之比。

### 3.2 修法：水汽柱质量

`ClimateLayerLayout::moisture_column_mass_per_area` 对模型**自己的**饱和曲线沿
标准大气廓线积分：

```text
M_q = ρ ∫ q_sat(T_ref − Γ z) / q_sat(T_ref) dz
```

`Γ` 是已声明的 U.S. Standard Atmosphere 1976 直减率，`T_ref` 是同一标准的海平面
温度。**不引入任何新系数**。低层得 2520 kg/m²，上层（6–10 km）只有很少。参考
廓线是固定的而非逐格的：输运搬的是强度量比湿，逐格变化的换算会破坏水量守恒。

每一处比湿到水量的换算都改用它：`apply_moisture`、上层凝结、层间水汽交换、
可用量限幅，以及守恒门的 `moisture_total` 与 `moisture_change_total`。热量与动量
仍用干空气质量。

### 3.3 单改一半会塌：阈值 0.9 是靠偏大的水量撑着的

只改质量，降水从 2.85 塌到 1.02、相对湿度升到 83%、蒸发跟着塌到 1.05。原因是
凝结率正比于柱质量，质量小 2.8 倍则同样的过饱和只下三分之一的雨，湿度堆积、
饱和差缩小、蒸发被扼住。**又一对补偿误差：偏大的水量在补偿偏严的凝结阈值。**

阈值 0.9 出自 SPEEDY（Molteni 2003），但那是 SPEEDY 的**层状云**闭合，它另有
一套对流方案产生热带大部分降水。P4 没有对流方案，这一条闭合要同时承担两条
路径，因此不能沿用 0.9。

### 3.4 锚：水汽滞留时间

改锚在一个 P4 可被检验的观测量上：Trenberth et al. (2007) 给出地球全球水汽
滞留时间约九天。柱质量修正之后扫阈值（强制 5 轮以排除早停干扰）：

| 阈值 | 可降水量 | 滞留时间 | 全球降水 |
| ---: | ---: | ---: | ---: |
| 0.90 | 28.0 | 19.1 d | 1.46 |
| 0.75 | 26.4 | 12.3 d | 2.15 |
| 0.70 | 25.8 | 10.1 d | 2.56 |
| 0.65 | 25.3 | 8.8 d | 2.88 |

可降水量由廓线独立定出，滞留时间是唯一的锚，降水因此是**检验而非拟合**。
这一轮扫描在湿焓修正（§3.6）之前，最终取值见 §3.8。

### 3.5 实测（发布态，Draft seed 42）

| 量 | 修前 | 修后 | 参考 |
| --- | ---: | ---: | --- |
| 可降水量 kg/m² | 65.6 | **25.6** | 25 |
| 滞留时间 | 23.0 d | **9.5 d** | 8–10 |
| 全球降水 mm/day | 2.852 | 2.685 | GPCP 2.81 |
| 全球蒸发 mm/day | 2.927 | 2.797 | ≈ 2.8 |
| TOA W/m² | −1.64 | −1.92 | 门 ±10 |
| Draft 时延 | 18.4 s | 19.0 s | 目标 10–20 s |
| Standard 时延 | 56.1 s | 57.5 s | 目标 ≤ 60 s |

降水结构也变好：北纬 32.5 度出现**副热带干带**（1.24 mm/day，修前 2.15），中纬
次极大与极区干燥都成形。热带雨带仍在 ±20 度而非赤道，那是 Task 2 的目标。

### 3.6 第三处耦合：湿焓守恒也要跟着改

凝结函数内部做的是**湿焓守恒**的饱和调整：凝结 Δq 放出的热按 `L Δq / c_p` 加热
同一份空气。质量拆开之后这个假设不再成立——水量是 `M_q Δq`，而它加热的是干空气
柱 `c_p M_dry`，所以升温只有原来的 `M_q / M_dry ≈ 0.34` 倍。模型**实际施加**的
潜热一直是对的（`apply_phase_change_latent_heat` 用的是已经按 `M_q` 算好的速率
除以干空气热容），错的是求解器内部那条焓曲线。

`large_scale_condensation_kg_m2_s` 因此多收一个干空气柱质量参数，内部按
`M_q / M_dry` 缩放焓曲线与其导数；传相同质量即回到单柱形式。修正之后阈值 0.70
给出的滞留时间从 10.1 天变为 **9.5 天**、降水 2.563 → **2.685**，两项都更好。

### 3.7 第四处耦合：配对水汽的相对门同样病态

上层的水汽柱质量只有干空气质量的约 4 %，层间水汽交换量随之缩小，而失衡的分子
是同一 f32 数组的量化地板（该数组还承载大 70 倍的蒸发与降水项）。实测失衡
641 kg/s、配对量 5.75e8、比值 1.11e-6 刚破 1e-6 门，而外部水汽通量是 4.0e10。

这与 A4 §6.4 的热量情形同型，按同一原则处理：分母改为"配对量 + 外部水汽通量"，
即该倾向数组承载的全部水量。比值降到 1.6e-8。报表与单元测试同步。

### 3.8 工作点：阈值取 0.70 而非 0.65

0.65 与 0.70 的滞留时间都落在观测区间（8–10 天）。取 0.70 的理由是产品级时延：
0.65 在 Standard 上多花一轮（6 轮对 5 轮），实测 62 秒破 60 秒线；0.70 两档都过
（Draft 19.0 s、Standard 57.5 s）。**在观测区间内用工程约束做取舍**，并在此写明。

顺带一提，轮数对阈值不单调（0.65 给 Draft 4 轮 / Standard 6 轮，0.70 给 6 / 5），
因为它取决于残差何时跨过 0.24，与 §3.9 是同一个判据问题。

### 3.8.1 语料实测（17 seed，A5 Task 1 发布态）

| 指标 | A4 收尾 | A5 Task 1 | 参考 |
| --- | ---: | ---: | --- |
| 全球降水 mm/day | 3.009 | **2.759** | GPCP 2.81（−1.8 %，此前 +7.1 %） |
| 全球蒸发 mm/day | 3.140 | 2.873 | ≈ 2.8 |
| 潜热 W/m² | 93.1 | **83.1** | Wild 70–85 与 Stephens 78–98，**首次同时落入** |
| TOA W/m² | −1.58 | −1.50 | 门 ±10 |
| 雨影失败 | 6/17 | **0/17** | A0 基线 1/17 |
| 增湿比失败 | 2/17 | 4/17 | A0 基线 6/17 |
| 季节相位失败 | 9/17 | 9/17 | 未变 |
| 洋流陆地泄漏 | 0/17 | 0/17 | 硬门 |

雨影失败清零是这次修改的独立验证：A4 §6.6.1 曾预测"水汽滞留时间偏长 2.6 倍会让
水汽在下雨前被吹得太远、削弱雨影"，滞留时间修对之后该度量直接归零。增湿比略退
（2 → 4），仍好于 A0 基线。

### 3.9 顺带发现：残差判据看不见水循环

扫阈值时多次出现只跑 2 轮就满足残差目标而停的情况，此时环流尚未起转（海面风
3.45 对收敛后的 5.36）。`relative_state_residual` 由温度主导，对水汽与风不敏感。
A4 §4.4 的海冰复测撞的是同一个问题。发布态（阈值 0.65）跑满 4 轮不受影响，但
这条判据本身需要在后续里程碑修。

## 4. Task 2 —— 经向翻转环流（2026-09-03）

目标是赤道辐合带。按"先测后钉"，先给探针加了两张表：`[circ]`（每 5° 纬带的
低层 / 上层纬向平均风 `u`、`v`、层厚距平、由 `v̄` 算出的辐散、纬向动量平衡的
`f v`、`r u` 与残差）和 `[qflux]`（纬向平均经向水汽通量的辐合，与 `P−E` 并列）。
本节数字除注明外均为探针管线（`causal_formation_fixture`，Draft seed 42）。

### 4.1 实测：低层的哈得来支是反的

| 纬带 | 低层 `v̄` m/s | 上层 `v̄` | 低层辐散 1e-6/s | 纬向动量残差 `r u − f v` 1e-5 m/s² |
| ---: | ---: | ---: | ---: | ---: |
| 17.5° N | +0.50（向极） | +2.04 | +0.01 | −11.8 |
| 12.5° N | +0.41（向极） | +2.06 | +0.28 | −9.7 |
| 7.5° N | +0.17（向极） | +1.56 | +0.54 | −6.1 |
| 2.5° N | −0.20 | +0.68 | +0.51 | −2.8 |
| 7.5° S | −0.61（向极） | −1.23 | +0.31 | −7.7 |
| 17.5° S | −0.72（向极） | −1.96 | −0.22 | −13.8 |

两层在赤道与 ±30° 之间**都**向极流动，赤道辐散、±17–35° 辐合——雨带正好落在
低层辐合开始的纬度。低层纬向动量平衡里，涡动闭合给出约 −1.2e-4 m/s² 的东向
扭矩，一天阻尼（`r u ≈ −1.0e-4`）吃不完，余下的由向极流动的科里奥利力
（`f v ≈ +2.5e-5`）补足。这是必然的：在阻尼–科里奥利平衡的一层里，任何东向
扭矩 `F` 都给出 `v = −f F / (r² + f²)`，即向极的埃克曼漂移。要压过它需要向赤道
的气压梯度力 `> f F / r ≈ 4e-4 m/s²`（15° 处），本模型没有任何热力项能提供
（低层热压耦合 16.7 m²/s²/K 需要 24 K/1000 km 的梯度）。

### 4.2 把扭矩挪出低层会让风塌掉

| 配置 | 海面风 m/s | 全球 E | 全球 P | 赤道 P | 上层 `v̄`（7.5° N）| 上层厚度距平 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 发布态（A5 Task 1） | 5.37 | 2.80 | 2.69 | 1.81 | +1.6 | −2.0 / +2.9 km |
| 只上层，柱扭矩守恒（×2.5） | 1.53 | 1.01 | 1.03 | 1.20 | +14.1 | −3.8 / +7.3 km |
| 只上层，单位质量加速度不变 | 0.91 | 0.53 | 0.56 | 0.65 | +1.3 | −1.2 / +1.5 km |

发布的近地面风几乎全部是低层扭矩的埃克曼层：扭矩一挪走，海面风从 5.4 掉到
0.9–1.5 m/s，蒸发与降水随之塌掉；柱扭矩守恒的变体还把 4 km 厚的上层推出 7 km
的厚度距平。所以低层扭矩必须留下，而它留下就意味着**低层支不可能在 10–20°
向赤道**。这与 P4 设计里"同一柱加速度施加于两层"的冻结决定一致，只是把它的
代价量化了。

### 4.3 成因链：层界面的回推压住了热力直接环流

低层的热压项 `c_T ∇T`（16.7 m²/s²/K）本可以驱动向赤道的低层支：热带纬向平均
梯度约 1.1e-6 K/m 对一天阻尼给出 1.6 m/s。但辐合会抬高低层厚度，`g' ∇h` 立刻
回推；两层各自的厚度松弛（7 / 12 天）太慢，稳态下回推比阻尼大两个量级：

```text
V = c_T ∇T / (r + g' τ H₁ / L²)，L = 3000 km：g' τ H₁ / L² ≈ 1.2e-4 ≫ r = 1.2e-5
```

于是热力环流被压到 0.05 m/s，剩下的就只有扭矩驱动的漂移。发布态里两层厚度
距平**同号**（赤道 −1.1 / −2.0 km，35° +2.6 / +2.9 km），说明整柱在正压地漂移，
层间没有任何翻转。

### 4.4 设计：轴对称斜压厚度闭合

把两层大气由辐散驱动的厚度倾向分解成正压与斜压两个模（Vallis 2017 §3.4）：

```text
正压份额  t_bt,i = (H_i / (H₁ + H₂)) (t₁ + t₂)
斜压交换  Q↑ = t₁ − t_bt,1 = (H₁ D₂ − H₂ D₁) / (H₁ + H₂)      （D_i = ∇·(H_i u_i)）
```

斜压部分的**纬向平均**当作穿过层界面的质量交换：低层减去 `Q̄↑`，上层加上
`Q̄↑`。这是 Gill (1980) 第一斜压模里"加热 = 一层的质量汇、另一层的质量源"的
形式；层界面在一个宏步内完成对流调整（Betts & Miller 1986 的 2 小时 ≤ 7200 s
宏步）。它不引入任何新常数：纬带取立方球格点自己的行（`2 × face_resolution`
个带），交换量是显式内核本来就算好的厚度倾向的带平均。两层之和逐格不变，
大气柱质量账本不受影响。闭合只作用于每宏步一次的声明慢倾向：快内核的 RK3 各级
保留未闭合的浅水辐散，纬向平均层界面因此滞后一个两小时宏步，而这点滞后的回推
（`g' Δt H / L² ≈ 1.5e-6`）远小于一天阻尼（1.2e-5），与被拆掉的 7 天回推不在
一个量级；C1 单层剖面不受影响。

只闭合**纬向平均**是实测逼出来的：把所有尺度都闭合（实验 E4）虽然也稳定，
但一旦加上对流闭合（§4.6）低层厚度就在一分钟内塌到负值——层界面回推正是
本模型对格点尺度湿反馈的唯一刹车。轴对称闭合只放开经向翻转环流这一件事，
非轴对称的辐散仍受层界面约束。

### 4.5 实测（发布态）

| 量 | Task 1 | **Task 2** | 参考 |
| --- | ---: | ---: | --- |
| 成形轮数 / 残差 | 6 / 0.086 | 6 / 0.081 | — |
| 海面平均风 m/s | 5.37 | **6.12** | 地球 6.6（Task 0 判为偏弱） |
| 全球降水 mm/day | 2.685 | 3.057 | GPCP 2.81，±7 % 上限 3.007 |
| 全球蒸发 mm/day | 2.797 | 3.192 | 潜热 92 W/m²：Stephens 78–98 内，Wild 70–85 外 |
| 水汽滞留 / 可降水量 | 9.5 d / 25.6 | 8.3 d / 25.5 | 8–10 d / 25 |
| 赤道降水（2.5° S / 2.5° N） | 1.81 / 1.81 | **4.37 / 3.21** | 4.6 |
| 7.5° S / 7.5° N | 3.17 / 2.88 | 4.23 / 3.60 | 4.6 |
| 最湿纬带 | 22.5° N 6.30 | 12.5° S 8.46 | 5° N 4.6 |
| 低层 `v̄` 7.5° N / 7.5° S | +0.17 / −0.61（向极） | **−2.29 / +1.70（向赤道）** | 地球低层支向赤道 |
| 上层 `v̄` 7.5° N / 7.5° S | +1.56 / −1.23 | +4.43 / −4.08 | 向极 |
| 赤道低层辐散（2.5° S） | +0.36e-6 | **−3.14e-6** | 辐合 |
| TOA W/m² | −1.92 | −2.21 | 门 ±10 |
| 时延 Draft / Standard（同机同时段成对） | 20.04 / 61.32 s | 20.10 / 61.51 s | 10–20 / ≤ 60 s；Task 1 记录 19.0 / 57.5 |

翻转环流成形：低层支在 ±15° 内向赤道、上层支向极、赤道低层辐合，赤道降水翻倍。
海面风补上了 Task 0 量出的缺口（5.4 → 6.1，地球 6.6），代价是蒸发跟着抬 14 %、
全球降水越过 GPCP 包络上限 1.7 %——Task 0 那对补偿误差（风偏弱 × 饱和差偏大）
被修了一半，饱和差偏大的那一半仍在（阈值 0.70 把层平均相对湿度钉在 70 %，
地球边界层 80 %）。

### 4.6 未闭合的两处，附实测

**一、输运算子的界限重分配抹平了辐合带的水量收支。** `[qflux]` 表：

| 纬带 | 经向水汽通量辐合 mm/day | `P − E` |
| ---: | ---: | ---: |
| 2.5° S | +10.3 | +1.4 |
| 7.5° S | +6.1 | −1.7 |
| 12.5° N | −3.5 | −0.4 |
| 17.5° N | −2.1 | −0.9 |

分辨的低层流在赤道辐合了 6–10 mm/day 的水汽，落地的 `P − E` 只有 1 mm/day；
信风带输出 2–3.5 mm/day，`P − E` 却只有 −0.4 到 −0.9。单调二阶输运
（`barth-jespersen-component-local-v2`）把每格夹在一环极值之内，超出的部分由
`conservative_bound_redistribution` **按连通分量全局重分配**——辐合进来的水
被送到全球任何"有余量"的格点。所以本模型里辐合从不变成局地降水，最湿的带
始终是蒸发最强的信风带（那里 `P ≈ E`）。

按 Kuo (1965) 把这份超出量就地降下（实验 E8：全尺度闭合；E9/E9b：轴对称闭合
+ 强制 5 轮）都在一两个周期内让低层厚度塌到负值（−2 到 −16 m）。成因：对流
凝结的潜热落在驱动气压的低层，`c_T ∇T` 拉来更多辐合、更多水汽、更多雨——
CISK。地球上这一环被弱温度梯度平衡（Sobel, Nilsson & Polvani 2001）截断：
加热被上升运动的绝热冷却抵消并输出。本模型的能量账本是定容热容的"显热 +
水汽潜热"，没有位能项，绝热冷却无处记账；而按 §6.1 的分工契约，经向热量输送
又归辐射先验所有。把这份热放到上层（上层只有 5 天的配对松弛）会在那里失控。
所以对流闭合要等两件事之一：能量账本加上层质量的位能（随质量连续方程一起改），
或者辐射先验改成真正的无输送先验（A4 §4.3 的里程碑）。**留作开放问题交用户
裁定。**

水汽随交换质量升降（"水汽搭乘"）也实测了两种：按层平均混合比搭乘（E5）给出
赤道 10.5 mm/day、全球 P +37 %、滞留 5.9 天、赤道相对湿度 48 %，量级失控；按
6 km 界面处的廓线湿度搭乘（E7，Task 1 的同一条廓线，界面比值 ≈ 0.05）只多提
约 1 mm/day，赤道 P 3.5–3.9，全球 P +16 %。两者都不接：前者太强，后者的收益
不值得多一条机制（YAGNI）。

**二、环流强度是地球的 4 倍，根在涡动扭矩的幅度。** 闭合的 `u'v'` 在 45° 达
700 m²/s²，观测柱平均约 30（Peixoto & Oort 1992），扭矩是地球的约 10 倍；它驱动
的上层支 4 m/s（地球 4 km 层平均约 1–1.5）、信风 10 m/s（地球 6–7）、信风带蒸发
7 mm/day（地球 4.5–5）。P 越过包络的那 1.7 % 与最湿带仍在 ±12–17° 都是它的
下游。这是 P4 设计冻结的闭合幅度（"C=2/3 取柯西界的三分之一"是一个界，不是
观测值），要动它得先量观测剖面重钉，本任务不碰。

顺带：把温度输运改成守常形式（对 `T ∇·u` 项的检验）的实验因温跃层温度撞
−5 °C 下限（−5.000002，舍入）中断，未得出结论；温度输运的同一处界限重分配
（热量被送到全球）是 §4.5 副热带 +7–8 K 暖距平的嫌疑之一，留给 Task 3 一并看。

### 4.7 语料实测（17 seed，语料管线）

| 指标 | A5 Task 1 | **A5 Task 2** | 参考 |
| --- | ---: | ---: | --- |
| 全球降水 mm/day | 2.759 | 3.310 | GPCP 2.81（**+18 %**，包络 ±7 %） |
| 全球蒸发 mm/day | 2.873 | 3.372 | ≈ 2.8 |
| 潜热 W/m² | 83.1 | 97.6 | Stephens 78–98 内（贴上限）；Wild 70–85 外 |
| 海面平均风 m/s | 5.76 | **6.77** | 地球 6.6 |
| TOA W/m² | −1.50 | −1.76 | 门 ±10 |
| 雨影失败 | 0/17 | 0/17 | 硬门 |
| 洋流陆地泄漏 | 0/17 | 0/17 | 硬门 |
| 增湿比失败 | 4/17 | 3/17 | A0 基线 6/17 |
| 季节相位失败 | 9/17 | 10/17 | 软门 |

海面风补上并略过地球值，是本任务最干净的一项收益（Task 0 量出的缺口 5.76 →
6.77）。降水 +18 % 是它的直接代价：Task 0 那对补偿误差（风偏弱 × 饱和差偏大）修了风、
没修饱和差。饱和差偏大来自 Task 1 的阈值 0.70 把层平均相对湿度钉在 70 %（地球
边界层 80 %），而阈值不能单独动（它锚着滞留时间，见 §3.4）。修饱和差就是给
湿度加垂直结构或加对流方案，即 §4.6 第一条的同一件事。**降水包络的这次越界
连同 §4.6 一并交用户裁定：要么接受 +18 % 换来正确的风与翻转环流，要么等对流
闭合一起落地。** 硬门全绿。

### 4.8 收尾时撞出的两件事

**一、32 seed Standard 扫描 31/32：一个种子在 P5 终态投影报「精确态陆海判定与
投影态不符」。** 成因与气候无关：`LandOceanKind::classify_exact` 把高程与海平面
都四舍五入到厘米再比较，而投影态在 f32 高程上重解海平面（候选搜索取相邻 f32），
一个恰在同一厘米内的海岸格点就可能在投影后翻到另一侧。这是分类器自身的分辨率，
不是身份断裂；任何改变气候（从而改变侵蚀终态）的提交都可能让某个种子踩到它。
修法：`to_wire` 只对投影高程与投影海平面相差超过一个量化厘米的格点报不符，同一
或相邻厘米内的差异视为分辨率内。不改求解器、不改任何身份值。

**二、时延。** 首版把闭合也放进快内核的每个 RK3 级，并按格点逐次算 `asin`：
Standard 一次生成约 4 万次调用 × 1.4 万格，实测 Draft 20.6 / Standard 64.6 秒
（Task 1：19.0 / 57.5）。把纬带改为工作区建立时算一次后是 19.2 / 61.9 秒。闭合
按其出处本就是每宏步一次的对流调整（Betts–Miller 2 小时 = 宏步），于是只留在
声明慢倾向里，快内核零成本（§4.4），得 20.1 / 61.5 秒——快内核那份只值 0.4 秒。
两档仍高于 Task 1 的记录，于是把 Task 1 的代码在同一台机器、同一时段重测：
**20.04 / 61.32 秒**。闭合的代价在噪声之内（+0.06 / +0.2 秒），差的是机器状态
（今天比 Task 1 测时慢约 7 %）。时延结论以同机同时段的成对测量为准，写进 §4.5。

### 4.9 出处

| 项 | 出处 |
| --- | --- |
| 两层浅水的正压 / 斜压模分解 | Vallis, G. K. (2017) *Atmospheric and Oceanic Fluid Dynamics*, 2nd ed., §3.4 |
| 加热作为第一斜压模的层间质量源汇 | Gill, A. E. (1980) *QJRMS* 106, 447–462 |
| 对流调整时标 ≤ 宏步 | Betts, A. K. & Miller, M. J. (1986) *QJRMS* 112, 693–709；Manabe, S., Smagorinsky, J. & Strickler, R. F. (1965) *MWR* 93, 769–798 |
| 哈得来环流：上层支由涡动动量通量辐散驱动，低层支由摩擦与气压梯度控制 | Held, I. M. & Hou, A. Y. (1980) *JAS* 37, 515–533；Schneider, T. (2006) *Annu. Rev. Earth Planet. Sci.* 34, 655–688；Walker, C. C. & Schneider, T. (2006) *JAS* 63, 3333–3350 |
| 涡动动量通量的观测量级与垂直结构 | Peixoto, J. P. & Oort, A. H. (1992) *Physics of Climate*, ch. 11 |
| 弱温度梯度平衡（对流加热被绝热冷却抵消） | Sobel, A. H., Nilsson, J. & Polvani, L. M. (2001) *JAS* 58, 3650–3665 |
| 水汽辐合全部降下的对流闭合（实测否定） | Kuo, H. L. (1965) *JAS* 22, 40–63；Kuo (1974) *JAS* 31, 1232–1240 |
| 通量形式输运在辐散流场中的守常问题 | Jöckel, P. et al. (2001) *Atmos. Chem. Phys.* 1, 51–63 |

## 5. Task 2b —— 辐合水汽就地凝结，潜热按弱温度梯度平衡输出（2026-09-03）

用户裁定（2026-09-03）：先修 §4.6 第一条（机制缺口），再回头看第 1、3 条（幅度）。

### 5.1 症结复述

单调二阶输运把每格夹在一环极值内，超出的水量按连通分量全局重分配。对定厚度层
里的强度量比湿，通量形式的 `q ∇·u` 项在辐合处的"超出"恰是辐合质量带进来、必须
垂直离开这一层的水，在辐散处的"不足"恰是补进来的下沉空气造成的稀释——两者都
是物理，不是数值毛刺。把它们抹到全球，赤道就收不到雨、信风带自己蒸发自己下。

而就地降下这份水（Kuo 1965）在 Task 2 的实验里两次塌层（§4.6）：凝结潜热落在
决定气压的低层，`c_T ∇T` 拉来更多辐合，正反馈失控。

### 5.2 设计

1. **水汽输运的界限改为"保留两侧"**：新增
   `advect_scalar_monotone_second_order_retaining_convergent_excess_cancellable`，
   面值仍经 Barth–Jespersen 限制、出流仍受正性限制，但最终不再夹到一环极值、
   也不再做分量重分配——配对边通量本身就守恒。超出上界的份额通过
   `SecondOrderTransport::convergent_excess` 交给调用方。温度输运不变。
2. **超出量就地凝结**：`apply_moisture` 把 `超出量 × M_q / Δt` 作为对流凝结率，
   与地形抬升凝结同路（可用水量限幅、陆地再蒸发的 Picard 一步、留存比例）。
3. **潜热不落任何一层，记为外部输出**：对流凝结的 `L P_conv` 在上升支释放，被
   同一上升的绝热冷却抵消（弱温度梯度平衡，Sobel, Nilsson & Polvani 2001），
   转为翻转环流的位能、由上层支输出——而那份纬向平均输送按 §6.1 契约归辐射
   先验所有，落到某一层就是重复计数，落到低层还会触发 CISK。能量账本
   （显热 + 水汽潜热）因此多一项显式外部汇 `−L P_conv`，闭合门不变。
   `convective_precipitation_rate_mm_s` 随倾向发布，`apply_phase_change_latent_heat`
   只加热非对流的那部分。
4. 大尺度（阈值）凝结、上层凝结、层间水汽配对交换一概不动。方程指纹 v16 → v17。

### 5.3 实测（发布态，探针 Draft seed 42）

| 量 | Task 2 | **Task 2b** | 参考 |
| --- | ---: | ---: | --- |
| 成形轮数 / 残差 | 6 / 0.081 | 5 / 0.092 | — |
| 全球降水 mm/day | 3.062 | **2.886** | GPCP 2.81（+2.7 %，回到包络内） |
| 全球蒸发 mm/day | 3.175 | 3.009 | ≈ 2.8 |
| 海面平均风 m/s | 6.10 | 5.74 | 地球 6.6 |
| 水汽滞留 / 可降水量 | 8.4 d / 25.7 | 8.8 d / 25.3 | 8–10 d / 25 |
| 辐合带降水（7.5° S，本种子的辐合带在 5° S） | 4.53 | **6.62** | 地球辐合带 4.6–6 |
| 2.5° S / 2.5° N | 3.74 / 3.65 | 4.27 / 2.66 | 4.6 |
| 最湿纬带 | 12.5° S 8.45 | 12.5° S 7.44 | — |
| 22.5° N / 32.5° N（副热带） | 6.08 / 1.02 | 5.48 / 0.99 | 1.6 / 2.0 |
| 低层 `v̄` 7.5° N / 7.5° S | −1.66 / +0.97 | −1.52 / +0.61 | 向赤道 |
| TOA W/m² | −2.19 | −1.92 | 门 ±10 |
| 时延 Draft / Standard（同机同时段成对） | 21.66 / 63.70 s | **19.88 / 58.49 s** | 10–20 / ≤ 60 s |

雨向辐合带集中、副热带减弱、全球总量回到包络内，且没有热带热带（`dT` 无失控），
说明"潜热记为输出"切断了 CISK 那一环。

### 5.3.1 语料实测（17 seed，语料管线）

| 指标 | Task 2 | **Task 2b** | 参考 |
| --- | ---: | ---: | --- |
| 全球降水 mm/day | 3.310 | 3.317 | GPCP 2.81（+18 %，包络 ±7 %） |
| 全球蒸发 mm/day | 3.372 | 3.324 | ≈ 2.8 |
| 潜热 W/m² | 97.6 | 96.2 | Stephens 78–98 内；Wild 70–85 外 |
| 海面平均风 m/s | 6.77 | 6.66 | 地球 6.6 |
| TOA W/m² | −1.76 | −1.62 | 门 ±10 |
| 雨影 / 洋流泄漏 | 0/17 / 0/17 | 0/17 / 0/17 | 硬门 |
| 增湿比 / 季节相位失败 | 3/17 / 10/17 | 3/17 / 10/17 | 软门 |

语料管线（初级地形，无因果成形）上全球总量几乎不变：Task 2b 改的是雨的去向，
不是蒸发；探针上总量回落是 seed 42 成形地形上信风略弱的副产品（§4.5 的坑一：
两条管线不可直接比较）。+18 % 的判断不变，仍交裁定（§4.7）。

### 5.4 仍未闭合：涡动水汽扩散把辐合带的水往外搬

`[qflux]` 表显示 7.5° S 的分辨经向水汽通量辐合 6.1 mm/day，`P − E` 只有 1.3。
现在超出量已就地下雨，差额来自 `ATMOSPHERE_HORIZONTAL_EDDY_MOISTURE_DIFFUSIVITY
= 1e6 m²/s`：按发布的比湿梯度估算，7.5° S 两侧的扩散通量约 8 与 12 kg/m/s，
在 550 km 宽的带上散度约 3 mm/day，正是差额的大头。这个扩散系数的出处（Held
1999；North 1975 等湿能量平衡模型）是**热带外**斜压涡旋的宏观湍流，热带并没有
这种向极搬水的涡旋。把扩散限制在热带外（或按 Eady 活动随 `|f|` 消失，与涡动
动量闭合同一依据）是下一个杠杆，本任务不动。

### 5.5 收尾撞出的第二处 P5 刀锋

32 seed Draft 扫描 31/32：一个种子在质量阶段报「primary relief is not the immutable
terrain this product was solved from」。探针拆开条件：高程逐格相等，差的是水量——
P3 库存 1.335e18 m³ 对 P5 终态 1.3349999999999997e18，相差 256 m³，正好是这个量级
上 f64 的一个 ULP。P5 地形字段携带的是终态精确求解的**实现水量**，质量阶段拿它与
P3 库存**逐位**比较（束校验与质量阶段各一处）；二分只在多数种子上恰好落在库存
那一位。与 §4.8 同类：任何改变气候（从而改变侵蚀终态）的提交都可能踩到。
根因在投影：`project_final_terrain` 把终态实现水量写进了本该携带库存的
`water_inventory_m3`。修法：投影改为写入求解所依据的 P3 库存（地形字段校验本就
按闭合容差 `WATER_VOLUME_RELATIVE_TOLERANCE` 检查实现水量对库存），`finalize`
里"终态与精确态一致"的逐位水量比较改为同一容差；两处身份检查保持逐位不动。

### 5.6 出处

| 项 | 出处 |
| --- | --- |
| 辐合水汽全部降下的对流闭合 | Kuo, H. L. (1965) *JAS* 22, 40–63 |
| 弱温度梯度平衡：对流加热被上升绝热冷却抵消并输出 | Sobel, A. H., Nilsson, J. & Polvani, L. M. (2001) *JAS* 58, 3650–3665；Sobel & Bretherton (2000) *J. Climate* 13, 4378–4392 |
| 定厚度层里通量形式输运的 `q ∇·u` 项与质量连续的一致性 | Jöckel, P. et al. (2001) *Atmos. Chem. Phys.* 1, 51–63；Lin, S.-J. & Rood, R. B. (1996) *MWR* 124, 2046–2070 |
| 热带外涡动水汽扩散的适用域（§5.4） | Held, I. M. (1999) *Tellus A* 51, 59–70 |

## 6. Task 3 修订 —— 热带外涡动扩散与动力幅度（2026-09-05，测量中）

本节落实用户裁定“先 2 后 1、3”的后两项，替代计划中过时的辐射先验重标定。
A4 §6.1 的分工、压缩比、Task 2 的慢倾向轴对称闭合及 Task 2b 的水汽／潜热处理
保持冻结。本节候选尚未作为发布常量冻结；先记录生产算子的测量。

### 6.1 本轮基线

`7a5c858` 原版五表探针（因果成形管线、Draft seed 42）复现：5 轮，残差 0.0923，
全球 P 2.886、E 3.009 mm/day，海面风 5.74 m/s、饱和差 4.35 g/kg，水汽柱 25.3
kg/m²、滞留 8.8 天，TOA −1.92 W/m²。7.5° S 的分辨水汽辐合 6.14 mm/day，
P−E 1.32；22.5° N 降水 5.48。原始日志在 `target/probe/a5-task3-baseline-probe.log`。
这不是语料管线的海面风，也不是产品时延测量。

### 6.2 待测的有出处候选

- 水汽宏观扩散保留原有热带外量级，以边中点的 `|sinφ|` 加权。Visbeck, Marshall,
  Haine & Spall (1997), *JPO* 27, 381–402，Eq. (2) 给出 `K ∝ σ L²`，
  `σ ∝ |f| / √Ri`；固定混合长度与 Richardson 数得到本模型的活动代理。
  这不是 Held (1999) 的完整扩散律，也不声称热带所有湍流消失。它只约束本项
  未分辨斜压涡旋闭合；原 `K` 是极点归一值，不添加人为热带边界或裁剪。
  每条边仍只计算一份配对水汽通量，保证全球守恒。
- 热压不能把底部测高响应 `gH/T` 直接当作层平均响应。对于 Boussinesq 柱内垂直
  一致的温度扰动，从静力关系积分并投影掉正压模，得
  `a(z) = (g/T_ref)(H_total/2 − z) ∇δT`；两层平均系数为
  `c_lower = g H_upper/(2 T_ref)`、`c_upper = −g H_lower/(2 T_ref)`。
  `1/2` 来自层平均，非效率参数；`H_lower c_lower + H_upper c_upper = 0`。
  按既有层厚及标准大气参考温度，该候选约为 +68.07 / −102.10 m²/s²/K。
  出处：Vallis (2017) 静力 Boussinesq 与两层模分解；Battisti, Sarachik & Hirst
  (1999), *J. Climate* 12, 2956–2964，Eq. (9) 同样区分底部与层平均热压响应。
  模型继续分别使用两层自己的温度梯度，因此上述柱平衡只在梯度相同时成立，
  不能把系数恒等式称为任意演化状态的柱力守恒。
- 涡动扭矩保留既有球面 Reynolds 应力散度与两层分布。用观测应力量级替代
  柯西上界的任意份额。已核查 Peixoto & Oort (1992) Fig. 11.8b：瞬变涡动年均
  应力在 45° N 约 7、45° S 约 −14 m²/s²，双半球对称量级约 10–11。
  本轮先测约 10 的读图参考值，不声称小数位精度；原闭合峰位固定在 45°，
  因此按同纬度比较，不拿观测的约 35° 峰值代入。幅度与上述热压配套测试。

**出处更正（显式修订 §4.6 的观测量级）**：原“柱平均约 30”不受原图支持；
Fig. 11.7b 的上对流层局部应力与 Fig. 11.8b 的整柱压力质量平均不是同一个量。
后者也没有乘 `cosφ`。原闭合地球工作点 `U_e = 65 m/s` 时峰值约 704 m²/s²，
比这次核对的同纬度观测量级高约七十倍。取值依据仅为动量观测，不用降水反拟合。
MIT 原图转载 PDF 的 SHA256：
`51a30a95bc024d18c05752ea240d4a5217b4bda60ef5c47de00675b5271f572d`。

**度量更正**：语料写入器的 `wind=` 实际调用 `vector_rms`，是全表面风矢量的面积
加权 RMS；探针 `[evap]` 才是逐月海面平均速率。既有 §5.3.1 的 6.66 不应直接称为
“海面平均风”并与地球 10 m 洋面均值比较。本轮保留原指标身份，比较时写清口径。

一手来源：Visbeck et al. 作者存档
<https://oceans.mit.edu/JohnMarshall/wp-content/uploads/2013/08/Specification-of-Eddy-Transfer-Coefficients_39.pdf>；
Battisti et al. 作者存档 <https://atmos.uw.edu/~david/bsh1999.pdf>；
Peixoto & Oort Fig. 11.8 的 MIT 教学转载 <https://pog.mit.edu/src/812/angular_momentum.pdf>。

### 6.3 拆分测量与热压坐标缺口

只改扩散的探针仍为 5 轮、残差 0.0942，海面风 5.74 m/s；7.5° S 的分辨辐合
6.87、P−E 5.38 mm/day（原为 6.14 / 1.32），降水 10.64；12.5° S 降水
5.84、22.5° N 降水 4.98，全球 P 2.922、E 2.997。去掉热带涡动扩散的外搬
之后，降水与辐合收支靠近，同时暴露原动力幅度仍过强。

配套候选（应力参考约 10、热压约 +68.07 / −102.10）在因果成形管线失败。
诊断为零起算 cycle 3 / month 1，低层 cell 1499（约 9.37° N）即将塌层；失败前
`h = −5953.409 m`、T 0.026 °C、风速约 25 m/s，地形／floor 均为 1939.953 m，
land=1，当月目标 −5.495 °C；上层同格 h 879.134 m、T 6.159 °C。
这不是末端一个 ULP 的刀锋：低层已真实耗尽，不能改厚度门。

代码核查发现热压坐标缺口：forcing 给地表温度减去 `Γ·orography`，两层状态均
继承该项，热压却直接把原温度的水平梯度当作等高面温差。仅背景直减率就制造
`−c Γ∇orography` 的低层下山力；增大 c 也同步放大这个非目标驱动力。
待测修正仅在热压读取时用 `T + Γ·orography`，其中 orography 必须与 forcing
共用原定义，不使用截顶 floor，也不使用包含海底的原始地形梯度。
温度状态、辐射、水汽、UI 和 Task 2 的轴对称慢闭合保持原契约；静态参考项在
热压的端点差分中自然相消。

依据：Simmons & Chen (1991), *QJRMS* 117, 29–58，
<https://doi.org/10.1002/qj.49711749703>，保留原温度预报变量、只在格点压力计算中
扣参考廓线；Klemp, Skamarock & Fuhrer (2003), *MWR* 131, 1229–1239，§2，
<https://www2.mmm.ucar.edu/people/skamarock/Papers/klemp_et_al_MWR_2003.pdf>；
ECHAM6.3.02 科学文档 Eq. (2.66)–(2.69)，
<https://redmine.hammoz.ethz.ch/attachments/download/878/echam6_scidoc_6.3.02.pdf>。
`T+Γz` 是本模型已声明线性参考廓线的代数特例，不是新增经验修形。

### 6.4 配套候选的结果与开放问题（未通过，不发布）

上述参考廓线修正在窄算子测试中消除了纯背景直减率的水平热压风；扩散的赤道
隔离测试也通过。测试均先在原实现观察到断言失败，再在实验实现中通过。

因果成形探针加入该修正后可以生成，但需要 7 轮、残差 0.1536；全球 P 2.586、
E 2.506，海面风 4.31 m/s、饱和差 6.33 g/kg、滞留 7.4 天。年雨峰变为
17.5° N 的 17.05 和 22.5° S 的 16.26 mm/day，暖偏约 7–8 K，赤道附近变干冷。
因此“本 seed 能生成”不是候选成功。

再次核对 A4 §6.1 分工发现：原温度输运仍搬运完整温度场，没有排除纬向平均
热量。为隔离这一缺口，另测 `Q_transport′ = Q_transport − <Q_transport>_纬带`，
仅投影大气温度输运倾向，放在辐射／相变之前；使用 Task 2 已有带划分和面积。
这是冻结分工的过程归属投影，不是最终温度重映射；参考热容空间恒定，各带输运
均值为零同时保持层的全局热量。保留非纬向风跨平均温度梯度产生温差的项，
所以不能简单换成“只输运 T−纬带均温”。

该实验的因果探针为 4 轮、残差 0.1668，全球 P 2.785、E 2.671，海面风 4.67、
饱和差 5.21，水汽柱 21.6、滞留 7.8 天；22.5° N 降水降到 0.49，但 7.5° S
雨峰达到 **34.52 mm/day**，2.5° S 的低层经向风约 −10.03 m/s。雨的全球总量
接近地球，但集中程度与风场不合格，不能只用全球均值宣称幅度修好。

随后启动既定 17 seed **语料管线**，在第三个 seed 7 失败并中断，未生成完整
新语料：

- seed 42：P 3.415473、E 3.285258、潜热 95.060、TOA −1.362、风 RMS 5.491748；
  中纬西风份额 0.004897（既有下限 0.55），雨影和地形增湿亦失败。
- seed 3：P 3.047569、E 2.927800、潜热 84.716、TOA −0.798、风 RMS 5.299809；
  中纬西风份额 0.005796，雨影和地形增湿亦失败。
- seed 7：低层 cell 310 非正厚度 −1.878418 m，硬失败。

**开放问题，交用户裁定**：目前没有测得既符合观测应力量级、又满足现有风场与
厚度硬门的配套工作点。需要决定是否将低层动量垂直结构、带均跨层交换与当地
薄层可用质量的一致性纳入 Task 3 的设计前置；这不是调大／调小一个常数已有
解的证据。原闭合按整层施加应力加速度，却给整层使用边界层阻尼；带均垂直
交换对每格施加相同值，不检查当地薄层供体。这两处是下一步诊断假设，尚未
通过受控实验确认为本次 seed 7 的唯一根因，不据此直接新增机制或钉参数。
本轮不回退 Task 2／2b，不放宽厚度、质量、能量或质量包络。

还有一个独立数值检查候选：非均匀温度的通量式输运是否依赖摄氏／开尔文零点。
完整常数场会被现有限界钉住，不能只靠常数场测试排除问题；该项未实测，未改动。
相关历史出处更正：Jöckel et al. (2001) 正确文献为 *QJRMS* 127, 1035–1052，
<https://doi.org/10.1002/qj.49712757318>，不是此前 §4.9／§5.6 的 *ACP* 1, 51–63。

### 6.5 实验保全与验证状态

所有未通过的生产候选已隔离，`src/` 恢复 `7a5c858`。当前只保留规格和计划修订，
没有提交或推送。完整候选补丁保存在 `target/probe/a5-task3-candidate.patch`，
SHA256 `e661d1d306de579cec2f5a481cc3376ddf5b3284372a34da7d0174df251b8873`，
已用 `git apply --check` 确认可以在基线上重放。补丁是诊断原型：常量位置、旧注释、
指纹、格式与完整门禁尚未收尾，不能作为发布实现直接使用。

原始日志均在 `target/probe/`：`a5-task3-baseline-probe.log`、
`a5-task3-eady-only-probe.log`、`a5-task3-paired-failure-terrain.log`、
`a5-task3-reference-pressure-probe.log`、`a5-task3-zonal-transport-projection-probe.log`、
`a5-task3-candidate-evidence.log`，以及两个 red 测试和 `a5-task3-atmospheric-tests.log`。
因候选已在既定语料硬失败，尚未进入全量 Release、两档 32 seed、成对时延或 UI
验收；上述探针耗时一概不作为产品时延结论。

## 7. Task 3 扩展诊断修订（2026-09-06）

用户已批准继续检查低层动量垂直结构及薄层交换一致性。§6.4 的范围问题已裁定，
不再就同一范围重复请求批准；新增具体机制和参数仍以生产测量与一手来源为准。
先复现 seed 7 的薄层失败，拆分水平厚度通量、带均交换和松弛项，再核查边界层
摩擦的垂直平均及层间质量所携动量。保持 A4 §6.1、Task 2／2b、压缩比和时延
契约；在最终地图、守恒及既定语料通过之前不冻结新的发布常量。

### 7.1 seed 7 的供体预算实测

复现管线为语料、单独 seed 7。零起算 cycle 0 / month 9，cell 310 位于 1.45° N，
floor=0、land=0.48057。低层失败前 `h=-5963.413 m`，可用厚度只剩约 36.587 m；
完整厚度倾向 −0.022839 m/s、快水平厚度倾向 +0.008024 m/s，差额的慢倾向在
一个宏步要求净抽走 222.210 m。上层对应慢倾向增加 284.892 m；差额包含两层
各自厚度松弛。由此确认：当时水平通量在补入空气，冻结的垂直慢汇仍持续抽取
超过供体库存的量，不能用末端厚度容差解决。日志：
`target/probe/a5-task3-seed7-thin-diagnostic.log`。

### 7.2 摩擦的垂直映射候选

Held & Suarez (1994), *BAMS* 75, 1825–1830，p1826 的
`k_v(σ)=k_f max(0,(σ−σ_b)/(1−σ_b))`，按每层质量积分后除以该层质量。
本模型等密度参考柱采用质量坐标，层界面 σ=0.4；沿该模型自己的坐标积分，
低层得到 0.25/day、上层为零。四分之一来自积分，不是拟合降水或风速的系数。
现有上层 0.1/day 不属于地表摩擦，不能留着再声称整套采用 Held–Suarez。
原文：<https://www.gfdl.noaa.gov/bibliography/related_files/ih9401.pdf>。
该映射只修正列积分摩擦量，不声称已经从层平均风重建完整的 10 m 垂直廓线。

### 7.3 层间质量与动量的联合约束

Audusse et al. (2011), *M2AN* 45, 169–200，§2、§5.3–5.4：界面质量通量必须
携带同一方向的供体动量；上下行都受供体预算约束，水平与垂直通量不能重复花
同一份质量。有限交换满足 `m_d′=m_d−δm`、`m_r′=m_r+δm`、`u_d′=u_d`、
`u_r′=(m_r u_r+δm u_d)/(m_r+δm)`。预算采用与水平通量相同的
`H_ref−floor+h`。原文：<https://www.numdam.org/item/10.1051/m2an/2010036.pdf>。
具体时间离散仍待受控测量；不能仅改最终状态或只限制垂直通量而忽略水平消费。

### 7.4 联合交换离散的探索结果（未冻结）

先试单独摩擦映射，因果探针在 P5 月径流支持域失败：cell 3815、month 4 为
4081.2795 mm，既有上限 4000 mm；未放宽边界。日志
`target/probe/a5-task3-hs-drag-probe.log`。

联合交换原型将声明的带均交换从冻结慢高度倾向中分离，先完成水平快步，再以更新后的
供体厚度执行局部 Modified Patankar Euler：令 `A=|Q|Δt`，实际搬运量为
`A_eff=m_d A/(m_d+A)`，接收层同时收到该份供体动量。
离散来源为 Burchard, Deleersnijder & Meister (2003), *Applied Numerical Mathematics*
47, 1–30，DOI 10.1016/S0168-9274(03)00101-6；公式复核见 Kopecz & Meister,
*On order conditions for modified Patankar-Runge-Kutta schemes*, Eq. (3)，
<https://arxiv.org/pdf/1702.04589>。这不是原常通量的无条件等价积分：相对削弱量为
`x/(1+x)`，其中 `x=A/m_d`；薄层处原来的纬带一致交换变成受当地库存约束。
Torlo, Öffner & Ranocha (2022) 的正性生产—消亡系统前提要求供体趋零时消亡项也趋零，
原诊断常通量不满足这一边界前提：
<https://davidetorlo.it/files/publications/Torlo2021StabilityPatankar.pdf>。
因此该试验包含显式的薄层闭合修订假设，尚需步长参考对照，不能凭正性就冻结。

因果 Draft seed 42 五表原型：4 轮，残差 0.1905，P 2.460、E 2.548 mm/day，海面平均
风速 5.17 m/s，饱和差 4.60 g/kg，水汽柱 23.1 kg/m²、滞留 9.4 d，TOA −0.97 W/m²。
但 42.5° N 低层纬向风 −4.93 m/s、上层 +8.09，中纬低层仍呈东风。
单独复现语料 seed 7 已不塌层：P 3.113627、E 3.189077、潜热 92.277、风 RMS 6.280630；
西风份额仅 0.011606，面缝风速比 4.427870 超门，增湿比与季节相位也未过。
语料 writer 的测试退出成功不代表质量门成功，本次也不是完整 17 seed 语料。
日志为 `a5-task3-joint-exchange-probe.log`、`a5-task3-joint-exchange-seed7.log`。

审计还发现原型的 `f32` 配对量化没有厚度域约束，可能吃掉理论正余量；非正供体不能静默
跳过。必须在接受通量前验证实际供体域、转移方向、恒速不变及变质量动量收支，不能末端裁剪。

### 7.5 动量起转时间的独立诊断

现有每轮只有十二个宏步，即一个动力学日。四轮温度残差达标不能证明观测量级扭矩已经
建立正压风。研发对照保留每个 phase 的原步长与季节强迫频率，仅强制增加轮数，记录
中纬上下层纬向风、厚度与实际厚度加权的柱经向输送；不改压缩比，不改变产品时延门。

因果管线首个气候求解的 35–55° 双半球面积平均、每轮第十二 phase 终点：第 4 轮上下层
纬向风 −5.526 / +8.645 m/s，第 12 轮 −5.573 / +9.241，第 48 轮 −5.620 / +10.070。
第 48 轮实际厚度加权柱向极流为 0.11464 m/s。延长后低层东风没有反转，因此缺口不能
仅归为温度残差早停。该离线求解故意越过产品轮数支持域，随后报告校验正确拒绝
`formation_cycles=48 > maximum=12`，没有发布地图或全套五表；未放宽报告契约。
日志 `target/probe/a5-task3-spinup48-probe.log`。这是研发终点诊断，不是新产品工作点。

### 7.6 海面应力重复与 bulk 通量候选

继续拆分生产侧动量算子，确认下层 Rayleigh 项已经声明为地表摩擦，而 lower–mixed
配对又按相对风施加一份线性阻力；前者直接向外部丢失动量，后者才进入海洋。
不能把两项都当成同一界面应力而叠加。

`a5-task3-surface-stress-baseline-probe.log` 在因果 Draft seed 42 第四轮逐 phase 读取
实际风和海流，用生产配对助手及当前 Rayleigh 速率计算海面应力；同一状态同时计算
Large–Yeager 中性 bulk 参考。首次气候求解的十二 phase：旧双阻力海面平均应力
0.1747–0.1965 Pa，对应 bulk 0.0412–0.0562 Pa，平均相对风 4.92–5.53 m/s。
这不是通过修改最终风速拟合结果，是同状态下的应力算子对照。

待测最小修正：海面只保留一份 `τ=ρ_air C_D |U_air−U_water|(U_air−U_water)`，空气
损失和海洋获得等反向；海面不再另加 Rayleigh。动量系数采用 Large & Yeager (2004),
NCAR/TN-460+STR，Eq. (6a)；实现直接组合 `C_D |U|`，避免平静海面除零。
原文 <https://www.aoml.noaa.gov/ftp/phod/pub/sklee/articles/core2/large_yeager_2004_core.pdf>，
§2.1 的观测汇编覆盖约 1 至超过 25 m/s，低风极限与光滑平板理论相容。仍采用现有
下层风作为中性近地面风的近似，未引入波浪或稳定度自由参数；热、湿系数保持各自来源。
Edson et al. (2013), *JPO* 43, 1589–1610，Eq. (1)、Fig. 10 提供独立中风观测复核：
<https://airsea.ucsd.edu/wp-content/uploads/sites/10/2020/03/Edson-et-al.-2013-On-the-Exchange-of-Momentum-over-the-Open-Ocean.pdf>。

Large–Yeager PDF 校验和：`ba4296bce2073ff8a50e45ce422c290ebf4b8bd9dccfaf7d36479f10b59274ae`，
印刷 p7–8 对应 PDF 零基页 13–14。候选采用 world 中
`neutral_surface_momentum_transfer_velocity_m_s` 作为系数唯一事实源，未作为发布常数冻结。
实现试验同时删除了 full / fast 两条路径中重复的配对动量实现，统一调用同一个助手。

去重后因果五表：4 轮、残差 0.2183，P 2.873、E 2.898 mm/day，海面平均风速 5.66 m/s，
饱和差 4.61 g/kg，水汽柱 23.1 kg/m²、滞留 8.0 d，TOA −1.03 W/m²。
`a5-task3-core-strict-seed7.log` 还将非正实际流体厚度与量化后的非正供体改为显式错误；
seed 7 通过生成，但语料 P 3.489659、潜热 103.770、西风份额 0.013253、面缝风速比
5.113558，雨影与增湿亦失败。应力去重有独立物理依据，但它并未解决整个 Task 3。

注意：历史 `[circ]` 表的 `r*u` 使用旧固定速率，不能用于新候选的摩擦收支结论；
其上下层风、厚度和水汽通量列仍是实测。新增私有 `[momentum-forces]` 在 §7.6 去重之后
也未更新 Rayleigh 诊断表达式，后续日志的该列不作为证据。发布探针必须从生产摩擦事实源读取。

### 7.7 柱质量松弛的对照（未采用）

当前独立高度松弛含总柱源 `R_lower+R_upper`。生产水平厚度算子与松弛同状态对照，
因果管线首个求解的第四轮十二 phase 均值，单位 m/day；纬带依次为双半球绝对纬度
0–15、15–30、30–60、60–90°：柱松弛为 −0.774、−5.764、−0.848、+14.495，
水平厚度倾向为 −31.746、+28.542、+14.391、−30.387。
日志 `a5-task3-column-mass-probe.log`；这证明存在列源，不证明它是东风的根因。

Gill (1980) §2 的两层加热/冷却解释支持层间质量交换，
<https://rainbow.ldeo.columbia.edu/~alexeyk/Papers/Gill1980.pdf>。先删除两层高度松弛作短控制，
10 轮残差 0.277802 超过既有门，未发布。再试只阻尼斜压模 `-P D P h`：两层质量源
等反向，并携供体动量进入同一交换步，原 Task 2 诊断 Q 不变。10 轮残差 0.244455 仍失败；
48 轮研发终点残差 0.222033，但中纬下层仍 −4.940 m/s、上层 +10.458，报告按原轮数上限拒绝。
因此两种候选都未采用，不能以去列源已修好风向收尾。PDP 原型保存为
`target/probe/a5-task3-pdp-relaxation-prototype.patch`；当前恢复去重应力候选的原高度松弛。
日志：`a5-task3-no-height-relax-probe.log`、`a5-task3-baroclinic-relax-probe.log`、
`a5-task3-baroclinic-relax48-probe.log`。

### 7.8 线性动量近似的适用域检查（待测）

核对历史真相：2026-08-03 球面环流 solver comparison 规格明确线性旋转浅水；
2026-08-17 P4 规格 §5 的动量式没有水平动量平流；2026-08-23 修订仅把连续方程改为
非线性厚度通量。当前不是漏实现历史规格，而是需要检查线性近似在新热压幅度下是否仍适用。

最小诊断只读取当前状态，对笛卡尔速度三个分量复用生产梯度，测
`a_adv=-P_tangent[(u·∇)u]`，不推进该项；按纬带报告均值及 RMS，并与同状态涡动、
Coriolis、压力加速度比较。笛卡尔梯度后切向投影保留球面曲率，不能直接把东/北风当普通标量
求梯度。依据 Held & Hou (1980), p516 Eq. (1)、§3 的线性近似讨论，
<https://courses.seas.harvard.edu/climate/eli/Courses/EPS281r/Sources/Hadley-cell/more/Held-Hou-1980.pdf>。
该诊断不引入其纬向平均热量输运，A4 §6.1 的热量分工保持冻结。

实测更新（2026-09-06）：十二 phase 的低层动量平流加速度 RMS（单位
`1e-5 m/s²`），按上述四个绝对纬带为 7.932、7.096、2.953、1.236；涡动项对应
0.161、0.236、0.363、0.621。实际接入平流的因果五表为 3 轮、残差 0.2311、P 3.047、
E 2.902 mm/day、海面风 5.72 m/s，但中纬低层仍是东风。轮数与原候选不同，不能用该次
探针作成对时延或严格质量归因。原型未采用，日志 `a5-task3-horizontal-momentum-probe.log`。
独立关闭下层／上层机械剪切交换、保留热湿交换的四轮对照触发 P5 径流支持域错误，
日志 `a5-task3-no-mechanical-shear-probe.log`；恢复原机械交换，不把它视为已证明的重复项。

### 7.9 热压垂直投影的修订：两层必须共用柱温度梯度

此前 §6.2 原型只验证两层热压系数满足参考柱质量加权为零，却分别乘各自层温度梯度。
这不满足推导：两层温度梯度不同时，仍产生额外柱平均热力加速度。最低成本的生产
endpoint 算子测试已复现这一错误，日志 `a5-task3-column-thermal-red.log`。

令固定参考厚度为 `H_L/H_U`、`H=H_L+H_U`，共同参考温度为 `T_ref`，
`α=g/T_ref`。对每层分段均匀温度的静力加速度积分，有
`a_L=a_0−α H_L ∇T_L/2`、
`a_U=a_0−α(H_L ∇T_L+H_U ∇T_U/2)`。
选择共同压力使参考柱平均内部热力加速度为零后，得到
`a_L=α H_U ∇T_col/2`、`a_U=−α H_L ∇T_col/2`，其中
`T_col=(H_L T_L+H_U T_U)/H`。系数取法不变，两个消费者必须读取同一梯度。
出处为 Battisti, Sarachik & Hirst (1999), p2959 Eq. (8)–(9) 的静力压力层平均，
<https://atmos.uw.edu/~david/bsh1999.pdf>；上述两层式是该静力关系加零柱模条件的显式代数
推导，不声称论文原文直接给出本式。

适用域仍为固定参考厚度、等密度 Boussinesq 垂直基底。地形参考补偿继续使用
`T_col + Γ z_reference`；不把实际变厚度塞入权重，因为那会引入权重梯度及移动界面项。
因此所保证的是参考柱热压力配平，不能扩张为任意地形截断后实际柱的零压力。
完整倾向与分裂步的热力 endpoint 修正同时改用这一构造；热力学状态和辐射先验不变。

此前共同加速度／共同速度投影仅是诊断原型，未恢复中纬西风，均已移出候选。
共同速度投影因果五表 4 轮、残差 0.2275、P 2.753、E 2.860 mm/day、海面风 5.48 m/s；
不能把纬带经向流积分变小当成通过生产有限体积柱散度约束。日志
`a5-task3-common-pressure-probe.log` 与 `a5-task3-common-velocity-probe.log`。
下一步先测本节的最小热压修正，不冻结额外压力投影架构。

实测：生产 endpoint 配平测试红转绿，但因果 Draft seed 42 的十轮残差 0.269845
超过既有门，低层中纬仍 −5.671 m/s。正确柱温度下，将同样的柱积分扭矩全部施于上层
得到残差 0.269945、低层 −5.643；仅在动量助手中跳过机械剪切交换（world 交换配置、
热湿过滤和算子均不动）得到残差 0.367126、低层 −5.693。两项独立对照均未采用。
日志 `a5-task3-column-thermal-probe.log`、`a5-task3-column-thermal-upper-torque-probe.log`、
`a5-task3-column-thermal-no-shear-probe.log`。上层集中只作为极限近似：Chai, Jansen &
Vallis (2016), §4c、Eq. (21) 支持涡动收敛偏高层，经 Ferrel 环流传到地表，
<https://repository.library.noaa.gov/view/noaa/29843/noaa_29843_DS1.pdf>，不支持声称观测恰为
全部集中上层。重新核验 P&O Fig. 11.8c，年均 stationary 柱平均应力仅约 0–2 m²/s²，
不能把 transient 的约 10 补成 30；不据此扩大观测包络。

### 7.10 两层共同自由面压力的高成本参考（未冻结）

原型继续暴露一个独立机制差异：原高度加速度分别为 `−g_L∇h_L` 与 `−g_U∇h_U`，
没有标准两层流体的共同自由面压力。为区分机制缺口与短时起转，离线参考改为
`a_L=−g∇(h_L+h_U)+g'∇h_U`、`a_U=−g∇(h_L+h_U)`。
这是被求解压力机制的修订，不声称与原独立压力仅相差步长。
来源为 Salmon (2002), *J. Marine Research*, §3 Eq. (3.1a)–(3.1b)，
<https://www-pord.ucsd.edu/rsalmon/salmon.2002.pdf>，论文 1 为上层、2 为较密下层。

参考取 `g'=g_L+g_U`，这是在 `δh_U=−δh_L` 下保持原层间压力加速度差的模态匹配，
不是声称旧常数对应观测密度差。原内模约 43 m/s 得以保留；外模的 CFL 参考速度按
`sqrt(g*(H_L+H_U))` 计算，约 313 m/s。只改快步 CFL 的参考，不改涡动速度尺度与压缩比。
两层实际厚度满足 `H_Lactual=H_Lref−floor+h_L`、`H_Uactual=H_Uref+h_U`；刚性底为
同一个 `floor`，故自由面 `floor+H_Lactual+H_Uactual` 的梯度确为 `∇(h_L+h_U)`。

因果 Draft seed 42 五表：4 轮、残差 0.1122、P 1.845、E 1.897 mm/day、海面平均风速
3.85 m/s、饱和差 4.87 g/kg、水汽柱 22.8 kg/m²、滞留 12.4 d、TOA −1.34 W/m²。
生成通过不等于质量通过，低层中纬仍是东风。首次求解逐轮中纬低层风为
−6.1、−4.400、−4.159、−3.966 m/s，上层第四轮为 +10.156。
日志 `a5-task3-common-surface-full-gravity-probe.log`。此次不是产品级成对时延测量。
下一项研发参考保留原 phase 步长，强制 48 轮检查动量起转；不放宽产品轮数报告支持域。

2026-09-07 续测确认：48 轮已运行结束，末轮残差 0.014235，首次气候求解中纬低层
−2.263 m/s、上层 +12.576，实际厚度加权柱向极流 −0.08242 m/s。第 44–48 轮低层
为 −2.283、−2.277、−2.273、−2.266、−2.263，已接近平台但没有转西风。
后续报告按原 `maximum=12` 正确拒绝 48 轮，没有发布完整地图；日志
`a5-task3-common-surface-spinup48-probe.log`。恢复产品轮数上限，比较期仍最少四轮。
本结果不支持仅靠延长起转或据简化平衡初猜就解决风向；暂停此类实现。

下一步只做实际柱角动量账本诊断。厚度使用非线性 donor 输运，动量无对应水平平流；
层间交换的参考质量账本也不等于实际变厚度质量账本。统一在完整闭球积分
`J=Σ A ρ H [R(n×u)_z + Ω R²(1−n_z²)]`，分别量水平 `u dH/dt`、Coriolis 与
行星角动量水平输运的配对、质量松弛，以及机械交换的实际／参考质量差。
纬带边界输运不应当作伪扭矩，因此这次以全球积分识别缺项。Task 2 的 Q 在原 full
中不含宏步末动量更新；补入供体动量生成元后 Q 的局部柱贡献抵消。瞬时诊断不等于
MPE 整步离散守恒证明，也不独自证明最终偏风的成因，后续须用短步前后 J 复核。

### 7.11 实际柱角动量账本与外部柱质量源对照

因果 Draft seed 42、共同压力参考的首次求解第四轮十二 phase 平均，单位 N·m：
水平质量输运的相对角动量项 `ΣρAℓ(u)D_h` 为 `+9.14735e19`，涡动参考质量扭矩
绝对和 `7.40220e19`（净和约 `5.14e10`）；层间机械交换在实际质量下的残差为
`−1.56025e19`。Coriolis 为 `−5.13125e19`，水平质量输运携带的行星项为
`+5.66929e19`，两者几何余项 `+5.38036e18`。独立高度松弛的行星项为
`−2.42811e19`，相对项 `−3.32360e18`。日志 `a5-task3-column-momentum-ledger-probe.log`。
缺失的水平动量平流必须与 `u D_h` 成套；但本次未配平项的全球符号为加西风，不能直接
声称它解释了中纬东风。全球账本也不能替代纬带边界输运诊断。

短步复核仍调用现生产分裂积分器，补入 Q 供体动量生成元的瞬时预测为
`9.53431816e19 N·m`。第一求解 phase 0，步长 60 / 30 s 的实际 `ΔJ/Δt` 分别为
`9.53529292e19` / `9.52946758e19`，相对差 `+0.0102%` / `−0.0509%`；
第二求解同样对照为 `+0.0701%` / `−0.0241%`。这支持诊断与实际推进相符，
不是新容差或阶数收敛证明（步长减半没有单调减小误差）。日志
`a5-task3-column-momentum-short-step-probe.log`。

下一项受控候选：仅删除 C2 大气独立外部高度松弛，保留 Task 2 的 ±Q、海洋松弛、
共同压力、柱温度、机械交换和原轮数／残差门。柱连续方程中的 `R_L+R_U` 可以维持
定常净柱经向输送；不同纬度增删干空气还会改变行星角动量，即便全球净质量源为零。
Salmon (2002) Eq. (3.1c)–(3.1d) 的两层连续方程无这种外质量源；Gill (1980)
pp449–450 的 Newtonian cooling 作用于斜压压力／浮力模，不支持对自由面总干空气质量
作独立增删。之前 R=0 与 PDP 对照均在独立压力和错误分层温度梯度下，本次是已修正
压力机制下的新条件对照。若改善，只能先归于独立 R 整体，不能宣称仅其正压部分为因。

实测：无外柱质量源的窄单元测试红转绿（`a5-task3-closed-column-red.log` /
`a5-task3-closed-column-green.log`）。因果五表 4 轮、残差 0.1336、P 1.772、
E 1.824 mm/day、海面风 3.69 m/s；低层中纬仍 −3.722 m/s，未解决质量目标。
日志 `a5-task3-common-surface-no-height-source-probe.log`。

### 7.12 共同压力与惯性密度的一致性修订

进一步核验 §7.10 的参考公式：Salmon 的精确非 Boussinesq 压力矩阵
`A=[[g,g−g'],[g,g]]` 要求 `ρ_U/ρ_L=1−g'/g`，密度加权后才对称。
当前模型两层使用相同惯性密度，因此不能直接套用该精确异密度矩阵。
生产快倾向的层间互易压力响应单元测试已复现差异，日志
`a5-task3-boussinesq-pressure-red.log`。本条显式修订 §7.10 的密度适用解释。

保持现有恒定惯性密度，静力 Boussinesq 形式为
`a_L=−g∇η−g'_B∇ξ`、`a_U=−g∇η`，其中界面 `ξ=floor+H_Lactual=H_Lref+h_L`。
平底势能密度 `V=ρ_ref[g(H_L+H_U)²+g'_B H_L²]/2` 给出对称压力矩阵；
有底地形时保留真实形状应力，不能据此要求地形场全球压力扭矩为零。
来源为 Vallis 的静力积分 Eq. (2.20) 与恒定惯性密度近似 §1.6：
<https://empslocal.ex.ac.uk/people/staff/gv219/ecmm719/lecnotes18n.pdf>。
`g'_B` 继续继承原有效内模参数之和，不宣称保留 §7.10 精确模型的密度比。

快模速度同步改用耦合矩阵特征值：
`trace=g(H_L+H_U)+g'_B H_L`、`det=g g'_B H_L H_U`，
`c_+²=(trace+sqrt(trace²−4det))/2`。CFL 逐单元读取实际高度距平，不扣非负地形底，
从而取深度的保守上界；不再把原 `sqrt(gH)` 当作本矩阵上界。参考层厚下约 317.57 m/s。
对称压力修复的是连续机制，离散梯度仍须接受既有数值一致性检查。

互易压力测试红转绿。因果五表 4 轮、残差 0.1330、P 1.790、E 1.848 mm/day、
海面风 3.70 m/s，低层中纬 −3.639 m/s，仍未达到质量目标。
日志 `a5-task3-boussinesq-pressure-green.log`、`a5-task3-boussinesq-common-pressure-probe.log`。
下一步按 §7.11 已量到的机械交换实际柱残差，先以窄测试钉住变厚度下等反向动量契约。

### 7.13 机械交换实际质量与水平输运离散检查

层间机械交换窄测试已复现参考质量配对的问题：将低层厚度减小、上层不变后，两层的
实际质量乘加速度不再等反向。改为该内部动量交换读取实际流体厚度，并与 Q 供体步共用
同一个正厚度校验助手；海面应力、热湿交换暂不改。测试红转绿，日志
`a5-task3-actual-momentum-pair-red.log` / `a5-task3-actual-momentum-pair-green.log`。
因果五表 4 轮、残差 0.1360、P 1.819、E 1.885 mm/day、海面风 3.74 m/s，
低层中纬 −3.596 m/s，尚未解决质量目标。日志 `a5-task3-actual-momentum-pair-probe.log`。
此日志中的旧 `mechanical_actual_defect` 诊断仍按参考质量重算；随后已改为复用生产质量
选择助手，不能把旧诊断列当成修正后残差。

水平动量候选先只做同通量诊断。复用生产每边 donor 质量通量 F，计算
`d(Hu)=−div(F u_donor)`，与同一个 `dH` 合成速度倾向，再作单元切向投影。
球面 Cartesian 有限体积与径向分量去除的直接依据为 Weller, Weller & Fournier (2009),
*MWR* 137, DOI 10.1175/2009MWR2917.1，§2a–b：
<https://www.met.reading.ac.uk/~sws02hs/publications/WellerWellerFournier_2009.pdf>。
原论文预报 H 与 Hu；本地一阶 donor 的半离散形式不自动证明现有 H/u 分开 RK 的整步性质。

诊断需同时核验：全局 `u dH + H du_adv` 与边几何角动量余项
`ΣρF[(r_receiver−r_donor)×u_donor]_z` 一致；动能倾向等于
`−ΣρF|u_donor−u_receiver|²/2`；薄接收格的入流率
`ΣF_in/(A H_receiver)` 给出的显式混合步长上限。直接 Cartesian 差与切向投影有上述
半离散耗散性质，不额外叠加 parallel transport；cell-center 杠杆几何余项须测空间加密，
不能声称全球角动量精确守恒。此时未把水平动量项接入推进器。

### 7.14 水平动量中心通量参考试验（未冻结）

§7.13 同状态 donor 诊断首次求解十二 phase 平均：动能耗散为 `−5.00186e14 W`，
约 `−0.98 W/m²`；`u dH + H du` 的角动量余项为 `−4.85399e18 N·m`，与边几何式一致。
接收格最小入流时间约 9466 s。日志 `a5-task3-donor-momentum-diagnostic-probe.log`。
这项额外数值耗散足以改变风场，不能无检查地与现有物理黏性叠加。

保留原生产 donor 厚度通量 F，仅以算术平均 Cartesian 速度形成动量通量
`G=F(u_i+u_j)/2`。出处为 Chandrashekar (2013), *CiCP* 14, 1252–1286，§1、§4.5，
DOI 10.4208/cicp.170712.010313a：<https://arxiv.org/abs/1209.4994>。
这是将原文无压质量／动量对流子系统中的密度替换为层厚的同构推导：每边
`Kdot=ρF[(u_j−u_i)·(u_i+u_j)/2+(|u_i|²−|u_j|²)/2]=0`。
现有厚度通量的插值保持不变，动量平均不得替换成距离加权或边切向插值。
球面单元切向投影不改变切向速度所受功率；角动量保留可计算的网格几何余项。
不宣称完整浅水总能量守恒或 entropy stable。

同状态中心诊断的动能残差约 `1e4–1e5 W`，相比 donor 耗散约低九个数量级；
角动量余项与 `ρF[(r_j−r_i)×(u_i+u_j)/2]_z` 一致。日志
`a5-task3-centered-momentum-diagnostic-probe.log`。此后接入推进参考，物理黏性不变。
逐单元以四条边累积 f64 倾向，避免新增全图 scratch；full 与 fast 复用同一实现。
primitive 转换只减水平 `u dH`，不混入 Task 2 的 Q 或重复其供体动量作用。

步长规划同时检查 `Σ|F|/(A H)` 局部交换速率，复用原快模 CFL 目标。
该速率是局部矩阵尺度估计，不是变厚度 RK3 稳定性证明；变厚度的半离散能量恒等式
使用随时间变化的质量度量，因此不能直接套纯虚谱 RK3 稳定界。必须继续检查薄层
支持域、实际整步能量误差与步长加密结果，再判断参考方案是否可用。

接入后因果五表为 4 轮，P 1.899、E 1.893 mm/day、海面风 3.74 m/s，低层中纬
仍约 −3.8 m/s；日志 `a5-task3-centered-momentum-probe.log`。缺失二次动量项的
生产 fast 算子测试红转绿，补充非均匀薄层的半离散动能测试亦通过；日志
`a5-task3-centered-momentum-green.log`、`a5-task3-centered-momentum-energy-test.log`。
这些是机制与窄离散证据，质量目标尚未通过。

### 7.15 共同压力参考的静力解释边界（显式更正）

§7.9 的参考柱中性热压与 §7.12 的共同表面压力没有重复添加高度梯度。
背景界面位移与独立热力扰动可以并存；但“完整自由表面静力参考”的称谓过强。
若固定平底参考层并给定真实顶部压力，静力积分给出
`B_L=g(H_L∇T_L/2+H_U∇T_U)/T_ref`、`B_U=g H_U∇T_U/(2T_ref)`。
当前内部热压等于 `B_i−∇S`，其中
`S=g[H_L²T_L+H_U(2H_L+H_U)T_U]/[2T_ref(H_L+H_U)]`。
这相当于只保留内部热模；若把高度重新解释为物理高度减 `S/g`，还须同步变换
连续方程，而当前实际层厚没有这种温变坐标项。来源仍为 Vallis 静力积分与
Boussinesq 近似（§7.12）。本次仅更正原型解释，不新增共同热力强迫。

令 `α=H_Lref/Href`、`η=h_L+h_U`、`χ=h_L−αη`，Task 2 的连续理想目标是
`带均 dχ/dt=0`，并不锁住 η。当前层差压力为
`−g'_B∇(αη+χ)+(c_L−c_U)∇Θ`。该闭合抑制轴对称界面抵消热压，符合冻结原意；
它不保证无风静力平衡，也不存在有风平衡必然无解的代数矛盾。
上述共同热力项差额不改变同状态层间加速度差，不能据此直接归因中纬低层东风。

### 7.16 恢复完整声明 Q 的供体交换参考（待验证）

当前修正条件下，已在水平 fast 推进完成后、垂直交换即将执行时测供体需求
`x=|Q|dt/H_donor`。因果 Draft seed 42 两次求解的 96 个原宏步最大 x 为
0.09362323，各步最大值的均值 0.05599113；日志
`a5-task3-vertical-transfer-demand-probe.log`。此前 MPE 对最紧单元少转约 8.56%，
即使供体充足也系统性削弱声明 Q。这个原型近似不能当作冻结 Task 2 的等价实现。

下一参考只删除这层缩量，恢复 `A=|Q|dt`，供体厚度减 A，接收厚度加 A，供体速度
不变，接收速度按实际质量与供体动量混合。对固定方向、常量 Q 的孤立子步，这是
供体质量／动量方程的精确积分；依据 Audusse et al. (2011) §5.2，
Eq. (5.108)–(5.113)，以及该局地子问题的代数积分。保留舍入前 `A<H_donor`
与平衡 f32 写回后严格正厚度检查，超额时拒绝，不裁剪通量。
仍存在起点诊断 Q、末端执行的分裂滞后，必须加密慢宏步并重新诊断 Q 作对照，
仅加密 fast 子步不能检验这项误差。此修订不回退 Task 2/2b，也不令水汽或潜热搭乘 Q。

窄测试已复现 MPE 少转声明量并红转绿。恢复后的因果五表为 P 2.126、E 2.024、
海面风 3.79，4 轮残差 0.1200；原宏步最大 x 为 0.10968694，未耗尽供体。
日志 `a5-task3-declared-overturning-red.log`、`a5-task3-declared-overturning-green.log`、
`a5-task3-declared-overturning-probe.log`。仍不满足降水与风速目标。

同一第四轮首 phase，从同一起点分别用一整步、二半步、四分之一步重新诊断全部慢
倾向并推进相同物理时长：第一求解的低层风 RMS 为 4.68253 / 4.70402 / 4.71166，
最低两层厚度约 2237 / 2202 m、2237 / 2212 m、2237 / 2216 m；平均 P 为
1.28306 / 1.25416 / 1.20179 mm/day，E 为 2.07113 / 2.06358 / 2.06282。
日志 `a5-task3-declared-overturning-refinement-probe.log`。这是局地宏步对照，不能
当作最终地图误差包络；风的变化约 0.6%，P 仍有约 6% 的步长依赖，后续发布候选
须做固定轮数和相同物理时长的最终态对照。

**跨管线否定结果**：语料 seed 7 在首轮第三个月的 cell 308 耗尽低层供体。
完整宏步、半宏步、四分之一步均失败（分别记录为 −74.277、−66.437、−18.730 m）。
半／四分之一参考维持每月总物理时长，每子步重新诊断全部慢倾向与 Q，通量按时长
平均；没有仅增加 fast 子步。最细档失败时纬度约 1.592°，地形底 3838.414 m，
供体仅余 21.759 m，声明交换 40.489 m。日志
`a5-task3-declared-overturning-seed7-evidence.log`、`a5-task3-half-macro-seed7-evidence.log`、
`a5-task3-quarter-macro-seed7-evidence.log`。不能以 seed 42 成功替代此失败，也不能声称
已靠步长解决。离线加密代码已保存，恢复原宏步；此动力组合不具备发布资格。

未采用按实际供体厚度横向分配同一带总 Q 的建议：虽然可使零供体零交换，并保留
带总量，但会添加非轴对称 `δQ=Qbar(H_d/带均 H_d−1)`，改变 §4.4 对非轴对称界面
响应的保护。Audusse 的本地固定垂直份额和 WTG 的本地热收支均不直接支持这种
横向分配。本任务保持用户冻结的 Task 2，不用此规则或 MPE 缩量掩盖失败。

进一步恢复 Q 的原声明慢高度倾向位置：各 RK 阶段同时推进 `D_L−Q`、`D_U+Q`，
并按该阶段实际厚度／速度加入供体动量生成元。full 与带声明 Q 的 fast 同时包含
该生成元，因此 `full−fast` 不重复留下一份冻结动量；Q 本身仍只在宏步起点诊断。
这移除了新增的末端 Lie 分裂，不改变冻结空间闭合。最初的精确交换函数仅保留为
窄测试的孤立子问题参考。

阶段同步候选仍在语料 seed 7 同一位置失败；将配置 fast 步长额外缩小到原值的
1/16 作为离线参考，仍失败。日志 `a5-task3-stage-q-seed7-evidence.log`、
`a5-task3-stage-q-fine-fast-seed7-evidence.log`。最细参考记录 H 从约 100 m 降到
不足 1 m，水平散度倾向趋向 `−0.000211 H`，而 Q 保持约 `+0.01782 m/s`，近地层
速度约 58 m/s。这是当前动力组合在该单元持续水平流出且垂直抽取的直接证据，
不能靠给当前轨迹再减 fast 步长提供支持域保证。已恢复原配置 fast 步长。
经典 Kutta RK3 的第三阶段含负组合系数，不能把 Euler 流出 CFL 直接称作 SSP
正性证明；本轮没有借此放宽正厚度检查或换积分器。

新增阶段动量生成元已用孤立冻结 Q 的精确供体积分做窄测试：包含地形截断的实际
供体、上下两个方向，一／二／四个 RK 步的最大速度差均在累积 f32 舍入范围内。
该子问题已到舍入量级，不能据此宣称测得整体三阶收敛。整步／两半步的孤立精确
交换对照及耗尽拒绝同样通过；日志 `a5-task3-stage-q-reference-test.log`。

### 7.17 应力幅度与扭矩纬向宽度的区分（读图诊断，未冻结形状）

P&O Fig. 11.8b 年均粗实线可读：NH 应力峰约 30–33°、11–12 m²/s²；SH 向极
应力峰约 35–38°、15–17。35°→55°，NH 约 10–12→−3 至 0，SH 向极应力约
15–17→2–5。这是原图分辨率支持的保守读图区间，不是原始数据数字化。
原图来源与校验和见 §6.2，<https://pog.mit.edu/src/812/angular_momentum.pdf> 第 6 页。

当前函数按 45° 应力约 10 标定时，35° / 45° / 55° 应力为 8.83 / 10 / 8.83。
其扭矩过零纬度约 35.26°，正加速度峰却约在 64.48°。
用生产同一个球面散度 `a=−[R cos²φ]⁻¹ d(S cos²φ)/dφ`，按 35–55° 两端
通量算 cos² 加权带平均，原图 NH 约 `6.0–8.1e−6 m/s²`，SH 约
`7.6–9.7e−6`，当前曲线仅 `2.72e−6`。因此同幅度下，该带扭矩弱约 2–3.6 倍，
不能只以 45° 应力标定替代扭矩标定。

同形状的旧应力约 700 对应该带扭矩约 `1.90e−4 m/s²`，即读图范围的 20–32 倍。
这支持旧扭矩过强，却不支持“精确十倍”的原表述。本节仍只比较年均 transient
柱平均，未混入 stationary 或上层局地峰值。下一步检查直接观测通量形状的来源
与可复核数据，不凭输出指标另拟 sin/cos 指数。

### 7.18 温度异常到实际层平均浮力的可证伪参考

阶段同步 Q 的细化参考仍发生供体耗尽，当前组合不满足发布不变式；本节候选对此
失败的因果作用尚待对照确认。继续做静力修正时存在
一个尚未被已有实现定义的字段语义：显示／热湿过程使用的两层温度，能否在动力中
近似代表实际层平均的温度扰动。Vallis 的静力积分要求后者，不能替应用字段自动
完成这种对应关系。本节提出具体类比，先按以下契约与生产反例验证，不视为已成立。
用户随后明确要求由代理按科学方法验证；撤回此前把技术假设提交用户裁定的请求。

候选沿用现有分层参考廓线，不新增温度字段、系数或可调参数：
`θ_L=T_L+Γ z_ref−T_0`、`θ_U=T_U+Γ z_ref−(T_0−ΔT_ref)`。
`z_ref` 复用 `atmospheric_reference_surface_height_m`，Γ 复用
`CLIMATE_OROGRAPHIC_LAPSE_RATE_C_PER_M`，T_0 使用现有标准大气海平面温度，
`ΔT_ref` 复用 `UPPER_ATMOSPHERE_EQUILIBRIUM_OFFSET_C`。这沿用初始化与配对热交换的
温度参考偏置；不会减去随纬度变化的辐射平衡温度，也不重新定义显示温度。

明确的近似是：把这两个已有场的参考异常，作为对应实际层的均匀浮力异常，
`b'=g θ/T_ref,K`，其中 `T_ref,K=T_0+273.15`。依据是 Vallis 的 Boussinesq 静力积分与当前分层参考异常模型的
类比；没有直接一手来源证明当前近地面代理对所有生成地形都代表层平均。
检验该映射时，移动界面必须保留其乘积微分项，而不能只把现有柱温权重换掉：

`D_T=(g/T_ref,K)[(H_L∇θ_L+H_U∇θ_U)/2+(θ_L−θ_U)∇ξ]`，
`a_L^T=H_U D_T/(H_L+H_U)`、`a_U^T=−H_L D_T/(H_L+H_U)`。

这里 H 均为实际正厚度；`ξ=H_Lref+h_L`，故 `∇ξ=∇h_L`，不是地形坡度。
新增界面项先扣除现有温度参考偏置；这不证明 `g ΔT_ref/T_ref,K` 等于现有
reduced-gravity 参数所代表的背景稳定度。把这些参考异常解释为实际层平均浮力，
仍是本节待验证的映射假设。仅保留内部热模，不补入 §7.15 的共同热压力。
保持 Task 2/2b 的诊断公式、Q 空间分配及既定应用时序；温度输运分工不变，
不新增水汽或热量随 Q 输运。动力状态改变后，诊断 Q 和凝结量可以自然变化。

最低成本契约：平底参考厚度且界面平时恢复原公式；起伏底、平接口／平顶、各层
温度异常各自水平恒定时热压为零；任意实际正厚度下 `H_L a_L^T+H_U a_U^T=0`。
后续是否修复 seed 7、是否使有效稳定度仍在支持域、能否满足质量与时延，均须实测；
本节不预先承诺通过，也不允许以该映射裁剪温度、厚度或强迫通量。

参考实现逐个 fast RK 阶段重算实际厚度的热压力，避免把随 H 变化的压力冻结在慢步。
full 与 fast 使用同一热压力算子，C2 大气端点不再重复添加旧冻结热压差；海洋端点保持。
暂时重算温度梯度的高成本路径只提供研发对照，待有质量收益后再优化固定温度梯度。

**推导边界与实测（2026-09-07）**：按固定物理高度先求梯度再平均，顶面项
`θ_U∇η` 在上下层之差中相消；接口项符号为正。上述按实际 H 去掉柱平均加速度，
是局地垂直模态截断，不等同于完整静力系统中任意共同标量压力的消去：被删向量
一般有旋度。这里的 θ 是参考温度异常，也不是大气潜温；不能用实际两层温差直接
诊断真实大气的静力稳定度。潜温／熵定义见 Vallis 讲义 §1.1 Eq. (1.11)–(1.14)，
<https://empslocal.ex.ac.uk/people/staff/gv219/ecmm719/lecnotes18n.pdf>。

冻结温度的局部静止压力快模令 `δb=(g/T_ref,K)(T_L−T_U−ΔT_ref)`、
`α=H_L/(H_L+H_U)`、`β=1−α`，压力矩阵为
`G=[[g+g_B−βδb,g],[g+αδb,g]]`，其中 `g_B` 为当前两层 reduced-gravity 之和。
`trace=H_L(g+g_B−βδb)+H_Ug`、`det=H_LH_Ug(g_B−δb)`；支持域为
`g_B−δb>0`。代码直接拒绝越域，不取绝对值或夹到正数。规划步长使用正特征值之和
的上界：舍去 trace 中为负的修正后，所得 trace 上界随两层厚度单调增加，可继续
通过不扣非负地形底来保守估计。该上界仅针对冻结温度的压力快模，不是含背景剪切、
温度梯度零阶项、Q 与经典 RK3 的整体稳定性证明。

窄测试：实际低层厚度改变后仍保持柱动量抵消；起伏底、平接口／顶面且参考温度
异常恒定时没有热压加速度，均通过。日志 `a5-task3-actual-thermal-projection-green.log`、
`a5-task3-actual-thermal-terrain-test.log`。
语料 seed 7 第一轮第二个月 cell 285 的有效 reduced-gravity 为 −0.39229369 m/s²，
候选失败。当地实际 H 为约 1146 / 5092 m，温度为 4.806 / −41.052 °C。
日志 `a5-task3-actual-thermal-seed7-evidence.log`。这否定当前映射组合在该已测状态的
适用性，不证明真实空气已经静力不稳定，也不作为调大背景稳定度的理由。

### 7.19 温度强度量输运的原点不变性（成因实测与参考修正）

在上述失败格的同一宏步起点，复用原生产二阶输运算子，分别输入摄氏温度和
转换后的开尔文温度；风、地形、网格与物理时长完全相同。低层原温度 −31.228 °C，
完整温度倾向给出约 +36.034 K，其中原输运为 +35.811 K，换开尔文却为 +0.0765 K。
上层原输运为 +2.145 K，换开尔文为 +56.050 K。两层全场最大原点误差分别为
45.215 / 53.905 K。日志 `a5-task3-temperature-origin-seed7-diagnostic.log`，语料管线
seed 7；不是五表管线，不与同 seed 五表直接相减。

原算子推进 `∂T/∂t=−∇·(uT)`，其散度项会将温标零点乘进收敛／发散过程。
该保守式适用于密度型量；温度是强度量，其平流部分为 `∂T/∂t=−u·∇T`。
出处：LeVeque (2002), *Finite Volume Methods for Hyperbolic Problems*, ch. 9；
Clawpack 的 [variable-coefficient conservative advection 示例](https://depts.washington.edu/clawpack/sampledocs/v570_gallery/pyclaw/gallery/variable_coefficient_advection.html)
明确把 q 定义为守恒量的密度。当前试验只修 C2 大气温度调用，不改水汽密度通量或
Task 2b 的就地凝结；海洋温度同样需要独立核查，本轮先隔离大气反例。

最低成本参考复用已有 intensive upwind 算子：接收侧贡献
`F_ji/A_i (T_j−T_i)`，只依赖温差；按每格总入流率确定 Euler 凸组合 CFL 不超过 1
的等长子步，不新增经验步长系数。该部分是有界的一阶参考，不声称比二阶精度更高。
随后仍按冻结分工投影掉温度输运倾向的纬向平均，参考热容下每带净输运热量为零；
因此不把压缩／膨胀效应偷偷归给热平流，也不添加新的绝热或对流热源。
验证顺序为：非恒定场温标平移与局地有界的窄测试、seed 7、五表；有质量收益后，
再用相同物理时长更细温度子步作离线最终态误差对照。不得用原来的错误密度输运
作为高精度参考，也不能凭单位不变性通过便宣称整个热压映射已获证实。

**参考结果**：强度量输运的非恒定温标平移、跨 CFL 无新极值、常量保持窄测试通过，
见 `a5-task3-intensive-transport-test.log`。替换大气温度调用后，语料 seed 7 越过
原先的内部稳定度失败，但第一轮第四个月 cell 309 仍发生低层耗尽（−0.4292 m）。
宏步起点当地实际低层约 115.34 m，Q 请求约 191.24 m，水平初始倾向也向外流。
日志 `a5-task3-intensive-temperature-seed7-evidence.log`。其中错误处理附带的
`[temperature-probe]` 仍特意调用旧密度式算子做原点对照，不能误读为新生产输运的值。

新组合的因果 Draft seed 42 五表：全球 P 2.608、E 2.518 mm/day，海面平均风
3.65 m/s、饱和差 5.94 g/kg，水汽柱 20.2 kg/m²、滞留 7.7 天，TOA −0.97 W/m²。
日志 `a5-task3-intensive-temperature-five-tables.log`。温度输运错误已单独确认，
但风仍弱且另一个 seed 的厚度不变式失败；当前不能冻结发布常量或进入最终验收。

同组合的慢宏步四分之一步参考仍失败：每个物理月总时长不变，四次重新诊断全部慢
倾向并按时长平均通量，第一轮第四个月 cell 308 低层厚度 −6.4680 m。
日志 `a5-task3-intensive-temperature-quarter-macro-seed7.log`；恢复原宏步后再继续。
带 `invalid-cached-build` 后缀的首次日志因复制文件保留旧时间戳、Cargo 未重编生产库
而作废，不参与结论；有效参考已确认重新编译。不能把更新频率提高直接称作解决。

### 7.20 观测通量形状的离线参考（未冻结）

§7.17 已确认旧解析应力的幅度标定不能修复峰位与宽度。本参考读取同一 P&O
Fig. 11.8b 的 YEAR 粗实线，按 0 至 80° 每 10° 刻度取整数 m²/s²；NH 北向应力
粗读为 `[1,4,8,12,10,2,−3,−2,−1]`，SH 对应负纬度、仍以北向为正为
`[1,−3,−9,−15,−16,−7,3,10,9]`。常规读图范围 ±1，50°S／60°S 陡坡与曲线
交叠处 ±2；这是图像读取范围，不是观测统计置信区间。来源与原 PDF 校验和见 §6.2。
节点事实源为 `EARTH_TRANSIENT_EDDY_STRESS_PROFILE`，半球奇对称化采用差的一半，
不能对两半球取绝对值；赤道由同一原始点相减自然为零。

对通量分段线性插值，再施加同一个球面散度定义；原图以外的 80–90° 极冠用
`S=S_80(cosφ/cos80°)²` 保持连续并按轴对称光滑应力的允许阶次趋零。该极冠延伸
仅为正则边界近似，不是额外观测；节点导数跳变、读图误差的差分放大均保留为
参考局限，不另拟曲线或平滑宽度。当前同网格量化后的净轴向扭矩修正沿用现有算子。
它是检验整条观测通量剖面的参考，同时改变低纬行为和高纬反号，不归因为单独的
中纬度宽度效应。零热对比仍不给涡动强迫，非地球热对比沿用当前 APE 速度平方比。

预设涡动通量并取散度的理想化环流对照有直接先例：Singh, Kuang & Tian (2017),
*JAS* 74, 467–486, §4 Eq. (3), DOI 10.1175/JAS-D-16-0238.1；
<https://singh.sci.monash.edu/papers/Singh_2017_Hadley_circulation_interactive_SST.pdf>。
该先例不证明本粗读地球形状对任意地图都准确。先跑同一轴向扭矩窄契约，再测 seed 7
与因果五表；只有实际支持域和质量通过之后，才评估是否足以作为发布闭合。

实测：已有极点正则／量化后净轴向扭矩窄测试通过，日志
`a5-task3-observed-stress-closure-test.log`。语料 seed 7 仍在同月同 cell 309 耗尽，
厚度 −0.3281 m，较原解析形状的 −0.4292 m 未发生质变；不能把形状偏差当作该处
耗尽的主要原因。日志 `a5-task3-observed-stress-seed7-evidence.log`。
因果五表仍为 5 轮：P 2.583、E 2.493 mm/day，海面平均风 3.63 m/s，饱和差
5.97 g/kg，TOA −1.00 W/m²。末轮 35–55° 低层均风从约 −3.14 改为 −1.84 m/s，
仍是东风；全球均风没有恢复。日志 `a5-task3-observed-stress-five-tables.log`。

### 7.21 高地温度代理的局地出流检查（测量设计）

高地增亮改变 `annual_surface_c`，两层目标都由它产生；加回参考直减率并不会
撤去地表反照率造成的非纬向冷异常。因此当前实际层热压将这份近地面冷异常复制
到两层浮力中。这是否参与薄层耗尽尚须量，不以字段语义疑点直接关掉热压。

只读测量在启动静止状态和耗尽前宏步起点进行：H、h、Q 与 forcing 保持不变，
复用 Task 2 纬带与面积，把 `T+Γz_ref` 分解为带均与非纬向偏差；用生产热压算子
求完整温度与带均温度两次加速度的差。同时取生产机械压力作为恢复力对照。
对两种加速度分别仅在快照副本上施加 1 秒／0.5 秒速度增量，再调用原厚度通量
算子，以 `−(dh_perturbed−dh_base)/ε` 量净出流加速度。只输出目标格与一环。
这个一侧有限差分包含可能的 donor 换向与 f32 舍入，不冒充解析导数或新的演化方案。
若非纬向热压加速出流并抵消机械恢复力，才据此继续审查整层浮力映射；否则削弱
这条嫌疑。全过程不改变 Task 2 的公式、空间分配、Q 数量或应用时序。

实测日志 `a5-task3-non-zonal-pressure-outflow-seed7.log`（语料 seed 7）：启动时
cell 309 低层非纬向温度异常约 −29.44 K，热压出流导数约 +2.97e−5 m/s²。
耗尽前该格实际低层 115.58 m，热压／机械项分别约 +5.47e−7／−3.44e−7 m/s²；
两种扰动时长结果接近。该格热压确实助推出流，但相邻 285、308 的两项均指向入流，
不能把单格符号推广为全域唯一成因。当地低层风约 45 m/s，上层约 7 m/s，继续
检验层间剪切应力的厚度依赖，不以此直接修改已冻结的地形反照率代理。

### 7.22 固定垂直黏性的受控参考（未冻结）

当前成对动量交换以 `min(m_L,m_U) Δu/τ` 定义应力。实际厚度为 6/4 km 时与
界面移到 1/9 km 时，总厚度、中心距、速度差均不变，但应力变为四分之一；
窄测试已复现，日志 `a5-task3-fixed-viscosity-stress-red.log`。这是固定交换时间的
本构结果，不称为原函数代数错误。它在薄层中相当于减小垂直黏性，需单独检查。

参考采用 Audusse et al. (2011), §3.5 Eq. (3.71)–(3.73) 的两层垂直黏性通量：
`S=2ρν Δu/(H_L+H_U)`。出处 <https://www.numdam.org/item/10.1051/m2an/2010036.pdf>。
保持参考层厚处原应力，取 `ν=(H_L0+H_U0) min(H_L0,H_U0)/(2τ)`，从现有层厚
与交换时间推导，不新增调参旋钮，也不声称原交换时间是地球观测值。复用原成对
交换 API 时，使用 `τ_eff=τ (H_L+H_U)min(H_L,H_U)/[(H_L0+H_U0)min(H_L0,H_U0)]`。
两层惯性密度相同，等价使用实际／参考柱质量的这一比值。

只改变 C2 大气层间机械交换，不改变空气—海洋应力或 Q 携带的供体动量。剪切衰减
率加入现有显式步长约束；有限应力仍可在趋零质量上产生很大加速度，不能宣称已
保证所有 RK 阶段正性。先验证参考强度、固定中心距应力和实际柱动量抵消，再跑
同一 seed 7。若仍失败，记录否定结果，不靠任意加大黏性获得通过。

结果：窄契约通过，日志 `a5-task3-fixed-viscosity-stress-green.log`；语料 seed 7
仍于第一轮第四个月 cell 309 耗尽（−4.2571 m），日志
`a5-task3-fixed-viscosity-seed7.log`。宏步起点层厚虽增至约 162.30 m、低层风下降，
Q 请求仍约 191.87 m，并未消除薄层失败。撤下固定黏性实验，原型源码保存在
`target/probe/a5-task3-fixed-viscosity-tendency.rs`；不将此假设叠加进下一项参考。

### 7.23 参考温度异常输运的语义对照（未冻结）

§7.21 的相邻海格 cell 310 低层温度异常约 −37.60 K，而年目标异常约 −4.27 K。
这不足以证明原因，但指出输运后的层温度与当地参考场相差很大。压力使用
`A=T+Γ_env z_ref`，输运仍使用 T；两者是否意在描述同一层浮力异常需受控检验。

只把 C2 大气输运输入改为 A、以 A 的增量更新 T，保持相同温差迎风算子和带均
倾向投影。此候选将 A 解释为解析的层浮力示踪量，地形直减率是显示温度的静态
参考映射。它改变了物理解释，不能冒充原方程的坐标恒等变换：原方案有
`D_h A/Dt=Γ_env u·∇z_ref`，候选把右端置零。也不称 A 为真实潜温，真实干空气
若跨坡速度满足 `w=u·∇z_ref`，应有 `D_h A/Dt=(Γ_env−Γ_dry)w`，一般不为零。
出处：Vallis 讲义 §1.1 热力学与潜温 Eq. (1.11)–(1.14)，
<https://empslocal.ex.ac.uk/people/staff/gv219/ecmm719/lecnotes18n.pdf>。

最小契约是在同纬带内有地形起伏和跨坡风、初始 A 均匀时，候选输运不产生新热压；
比较有风／无风的生产温度倾向差，消去相同辐射及层间热交换。窄测试只证明这一
变量语义，不证明它是真实空气热力学；随后同 seed 7 检查是否影响现有失败，不
调直减率、不改辐射先验、不修改 Q。结果决定是否值得继续物理适用域验证。

结果：参考场跨坡输运契约红转绿，日志
`a5-task3-reference-temperature-transport-red.log`／`green.log`；但语料 seed 7
仍在第一轮第四个月 cell 308 耗尽（−4.7957 m），日志
`a5-task3-reference-temperature-transport-seed7.log`。候选没有解决原始不变式失败；
撤下这项额外的变量语义改动，保留 §7.19 已确认的温标原点修正。参考源码另存
`target/probe/a5-task3-reference-temperature-transport-tendency.rs`，不能以新契约
测试通过便认定它是当前产品场的正确物理解释。

### 7.24 正厚度静止参考的可达性（诊断，不是新门禁）

复核 §7.18 的内部热模：两层共同温度异常 C 并没有被投影全部删除；仍有
`a_L^T=κ U∇C/2`、`a_U^T=−κ L∇C/2`。被删的是完整静力热力加速度的柱平均，
它一般不为零，也不等价于无影响的标量规范变换。当前内部差模没有新发现的底坡
漏项；完整共同热压是已声明的截断，不把它包装为修正了完整自由面静力系统。

由生产所用机械／内部热压力公式，水平无风平衡满足
`[g_B−κ(θ_L−θ_U)] dξ=κ(L dθ_L+U dθ_U)/2`，
`dη=−(g_B/g)(L/H)dξ`，其中 `L=ξ−b`、`U=η−ξ`、`H=L+U`、κ=g/T_ref。
这是 §7.18 模型的压力平衡判别，来源框架为 Vallis 讲义 §2.2–2.3 的静力分层式；
不是新增经验常量或对完整旋转环流的稳定性定理。

若固定平底且 θ_L=θ_U=C，积分上述两式，首次触底所需的共同冷差为
`|ΔC_contact|=2 sqrt(g g_B)/κ atan[(L_0/H_0)sqrt(g_B/g)]`。
采用既有参考界面／顶面及恢复力，底高 3.5 km 时约 17.11 K，平海底约 26.55 K。
这说明共同冷差达到 30 K 时，当前映射的这一静止参考已经没有始终正厚度的分支；
减小时间步不能创造这个分支。这里说的是水平无风平衡，不是仅有垂直静力条件
就能决定界面高度。带均异常也不能自动当作沿路径的温差。

进一步使用 §7.21 同一启动快照的相邻暖海格 310 与高地格 309：共同异常差约
−25.1699 K、底面从海面升至 3516.09 m。仅对这条边作线性温度／底面路径参考，
从暖格原参考界面／顶面开始积分上式。64／128／256 等分 RK4 的首次触底路径
份额分别约 0.707118／0.707114／0.707112，均未到高地端。该离线路径是压力
平衡诊断，不是修改生产地形、温度或强迫，也不是新的气候验收阈值。

实际失败快照两层温度已不同，而且科氏力、平流、摩擦及 Q 均非零，能够参与
压力平衡。因此不能由此宣称已证明完整连续模型必然耗尽；它提供的证据是，
把现有近地面冷异常复制成整层浮力，并不自动落在这个两层模型的支持域内。

回查 Battisti, Sarachik & Hirst (1999)，Fig. 1、§2a Eq. (4)–(9) 区分近地面
温差的垂直衰减与层平均压力；§2b Eq. (13)–(16) 则限定虚潜温异常在边界层内，
且显式忽略相对界面高度／逆温强度的高阶扰动。其机制可提供参考，但不支持把
生成器的高山近地面冷异常直接当作整个两层大气的均匀温差。
原文 <https://atmos.uw.edu/~david/bsh1999.pdf>。后续应验证浮力场的垂直表示及其
支持域；目前没有有证据的发布映射，不能继续用涡动扭矩或新的黏性系数补偿它。

### 7.25 浮力场的垂直表示：冷异常限于低层、暖柱保持自身层结（已实测，未冻结）

§7.24 的待验证问题已按科学方法核查。代码事实：`role_reference_temperature_c` 把上层
参考温度定义为当地气温目标减固定偏移，初始化与配对热交换共用它，因此地表反照率／
地形造成的非纬向冷异常在启动时就被复制进整个 10 km 柱，交换又把它维持在那里。
§7.24 的无正厚度分支正是这个表示的后果，不是热压算子的漏项。

有出处的垂直表示：Lindzen & Nigam (1987), *JAS* 44, 2418–2436 与 Battisti, Sarachik &
Hirst (1999) Eq. (4)–(5) 把非纬向（涡动）温度异常限定在 700 hPa 参考高度（约 3 km）
以下、线性衰减到零，自由大气只保留纬向平均结构 `T(y, z)`；本模型层界面在 6 km，
高于该参考高度。暖异常不适用这一限制：BSH99 §2b 区分 convecting 与 not convecting
两种状态，暖柱对流把边界层异常排入自由大气（Gill 1980 第一斜压模）；Manabe,
Smagorinsky & Strickler (1965), *MWR* 93, 769–798 的对流调整把不稳定柱调整到稳定
参考直减率——本模型对每对层已声明的固定偏移就是这条参考层结。据此上层参考取
**纬带平均与当地海平面归约气温目标中较暖者**，再按当地参考高度 `Γ z_ref` 折回，
使两层共用同一地形直减率坐标（界面浮力差中两项直减率相消），偏移
`UPPER_ATMOSPHERE_EQUILIBRIUM_OFFSET_C` 不变，未新增常量。纬带复用 Task 2 的
逐行带划分，带均对月目标逐月计算，年均初始化先取十二个月平均再求带均。

**静止支持域（离线，§7.24 同一方程）**：脚本复现原共同异常数字（边 310→309 触底
份额 0.7073，平底 26.55 K、3.5 km 底 17.11 K）后，改为仅低层携带异常：同一条边
不再触底，高地端低层仍余约 1116 m；固定平底与 3.5 km 底在 200 K 以内均无触底。
另测 LN 线性衰减（`H_b = 3 km`，层均因子 `1 − L/2H_b`）余约 1812 m；该因子需
新增高度常量，本轮不采用，仅记录为余量参考。

**窄契约（红转绿）**：`upper_atmosphere_reference_confines_cold_surface_anomaly_below_interface`
——半球高地 3.5 km、附加 20 K 反照率型冷异常的夹具：高地上层海平面归约温度等于
带均减偏移，海面上层等于当地减偏移，两柱在配对交换中均为零通量，只有高地柱
更稳定且稳定增量等于当地对带均的冷差。原 `atmospheric_reference_lapse_does_not_drive_horizontal_pressure_wind`
夹具改为像生产强迫一样把直减率写进目标场，而不是初始化后再减，契约本身不变。

**中间否定结果**：只取带均（不含暖侧下限）时语料 seed 7 通过，前 12 个 seed 完成，
seed 61 在第六轮第八个月 cell 2454（62° N、陆地、地形 1027 m）触发内部层结不稳定，
有效 reduced-gravity −0.0752 m/s²：当月当地目标 +30.7 °C，带均被海洋压低，
`T_L − T_U = 36.5 K`。日志 `a5-task3-upper-reference-corpus.log`。这就是暖柱必须
保留自身层结的直接反例。

**加暖侧下限后的语料（17 seed，语料管线）**：全部 17 个 seed 首次在 Task 3 全部
完成，无厚度或层结失败，雨影／洋流泄漏硬门 0/17；均值 P 4.537（2.42–7.60）、E 4.438、潜热 128.4、TOA -0.58、风矢量 RMS 6.14（4.16–9.06）。
软门失败：中纬西风份额 14/17、地形增湿比
5/17、季节相位 5/17、
面缝风速比 1/17。日志
`a5-task3-convective-reference-corpus.log`。降水与风远未达标，不是发布候选；
本条只解决 §7.24 的支持域问题。

**因果五表（Draft seed 42）**：5 轮、残差 0.0637，海面平均风 3.49 m/s（§7.20 为 3.63），
海面 E 2.797（2.930），7.5° S 降水 30.99（32.41），42.5°／47.5° 低层纬向风
−2.11／−2.10 m/s（−2.16／−2.03）。该 seed 成形地形少有带均高于当地的冷高地，
所以改动几乎不改变探针；赤道雨带过度集中与中纬东风是既有开放问题，不归本条。
日志 `a5-task3-convective-reference-five-tables.log`。

接口变化：`LayeredClimateState::from_annual_mean_forcing_cancellable` 增加海平面参数，
`role_reference_temperature_c` 增加上层参考输入，带划分助手移入 `state.rs` 供初始化
与倾向共用。边界：下限只调整参考，不调整状态，输运仍可在一个宏步内把柱推过中性，
原层结错误保留；低层自身的 LN 层均因子未采用；带均分辨率为网格行。工作区中
`atmospheric_baroclinic_pressure_mode_is_column_mass_neutral` 仍断言旧的一日阻尼，
与 §7.2 原型的 0.25/day 冲突，待阻尼决定一并处理。

### 7.26 低层纬向动量收支的带均分解（实测，未改机制）

为回答 Task 3 第二项“压力梯度应承担合理的信风驱动”，新增临时研发量测
`print_zonal_momentum_budget_probe`：在每个宏步起点，用同一状态逐项调用生产算子
（涡动闭合、热压、独立层压力加共同表面压力、水平动量输运、水平黏性、配对交换拆成
海面应力与层间机械交换、声明 Q 的供体动量、科氏力、陆面 Rayleigh 阻尼），把低层
加速度投影到东向，按面积平均到双半球 35–55° 与 10–20° 两带，并与完整声明倾向
求残差。残差在每个月均小于 `1e-7 m/s²`，说明分解闭合。逐月记录后按轮取十二个月
平均，脚本见 `target/probe` 同名日志的处理方式；单月快照的科氏项随季节强迫摆动
两个量级，不能单独引用。日志 `a5-task3-u-budget-monthly-five-tables.log`
（因果 Draft seed 42，本节数字取首个气候求解第 5 轮，单位 `1e-6 m/s²`）。

| 带 | u 低/上 m/s | v 向极 低/上 m/s | Q 向上 mm/s | 总 | 涡动 | 科氏 | 陆阻尼 | 海面应力 | 层间交换 | 热压 | 机械压 | 输运 | 黏性 | Q 供体 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 35–55° | −2.09 / +10.99 | −0.29 / +0.25 | −0.56 | +7.4 | +7.8 | −29.0 | +2.2 | +1.2 | +20.8 | +0.7 | +1.3 | +0.6 | −0.5 | +2.3 |
| 10–20° | −2.06 / +7.23 | −1.41 / +1.95 | −13.9 | +5.8 | −4.0 | −47.1 | +5.7 | +1.3 | +15.8 | −3.2 | +4.3 | +6.9 | −3.1 | +29.2 |

**信风带（10–20°）**：驱动项是经向压力梯度所致的向赤道低层流经科氏力得到的
东向减速 −47，与地球近地面 `v ≈ −1 到 −2 m/s` 的量级一致；所以“压力梯度未承担
信风驱动”不成立。抵消它的不是摩擦（陆阻尼 +5.7、海面应力 +1.3），而是上层西风
动量的向下注入：声明 Q 在该带向下（−13.9 mm/s，约 1.2 km/day）携带上层动量
+29.2，再加五日层间机械交换 +15.8，两项合计抵消科氏项的 95 %。地球副热带下沉
约 5 mm/s 且位于 20–30°；本模型的下沉紧贴 7.5° S 的雨峰（§7.20 五表 `div_low`
在 2.5–7.5° N 为 +5.4／+5.8e-6/s），是围绕单带雨峰的窄而强的翻转，不是行星尺度
哈得来环流。因此信风偏弱的直接成因是翻转环流的位置与强度，而它与赤道雨带过度
集中同源。

**中纬带（35–55°）**：低层向赤道 −0.29、上层向极 +0.25，是延伸到中纬的热力直接
环流，不是费雷尔环流（地球低层向极、上层向赤道）。低层西风来源为层间交换把
上层西风混下 +20.8 与涡动扭矩 +7.8，合计 +28.6；向赤道低层流的科氏项 −29.0
几乎全部抵消，阻尼项因风速小而可忽略。u 逐轮 −5.4 → −2.1 仍在起转，但平衡点
由 `v` 的符号决定：只要低层保持向赤道，西风源全部转成经向环流的科氏平衡，
u 停在零附近；观测形状扭矩（§7.20）已接近 P&O 的柱平均量级，单独加大扭矩只会
加强这一直接环流。费雷尔环流需要上层涡动扭矩由向赤道上层流的科氏力平衡、
低层由连续性向极；本模型上层在该带向极流动，说明热力直接强迫压过了涡动强迫。

**对雨峰的核对**：同一探针 7.5° S 带 `T_air` 高于目标 +4.6 K、比湿 20.5 g/kg、
降水 31 mm/day。Task 2b 只把辐合超出量的凝结潜热记为外部输出，Task 1 的大尺度
（相对湿度阈值）凝结潜热仍进入本层；31 mm/day 对应约 900 W/m² 潜热，其中进入低层
的份额足以维持 +4.6 K 暖异常，再经 `C2_LOWER_ATMOSPHERE_THERMAL_PRESSURE_M2_S2_K`
（约 68 m²/s²/K）在一个格距上产生 `1e-4` 量级的辐合加速度。这是 §4.6 记录的
CISK 型反馈在当前热压幅度下的表现，尚未分项实测潜热去向，本节只指出验证目标。

下一步验证顺序（均为测量，不预设机制）：(1) 按带分解 7.5° S 的潜热去向（大尺度
凝结进层、对流凝结外输）与热压反馈；(2) 以 §7.16 的 Q 诊断按纬度输出翻转质量
通量，与地球副热带下沉量级和位置对照；(3) 中纬两层各自的经向动量平衡，确认
上层向极流由哪一项压力驱动。在这三项之前不调涡动幅度、层间交换时间或阻尼。

### 7.27 雨峰潜热去向与上升支记账的受控对照（实测，未采用）

**分项实测**（因果 Draft seed 42，第 5 轮十二个月均值，`[tropic]` 探针，日志
`a5-task3-tropic-latent-five-tables.log`）：7.5° S 带降水 32.0 mm/day，其中对流
（辐合超出量，Task 2b 外输）21.5、地形 1.2、大尺度（相对湿度阈值）约 9.2；进入
低层的潜热 247 W/m²，即 2.9 K/day，维持低层高于月目标 +4.5 K；该带声明 Q 向上
77.7 mm/s，2.5° S 为 53.7，下沉支在 2.5° N（−50.0）、7.5° N（−30.9）与 12.5° S
（−25.4）。第 1 轮同带为 P 16.6、dT +0.8、Q 27.5，说明这是逐轮增长的正反馈：
辐合水汽在阈值内的大尺度凝结把潜热留在本层，热压（约 68 m²/s²/K）把暖异常变成
辐合加速度，翻转再增强辐合。地球 ITCZ 年均上升约 10–20 mm/s；按弱温度梯度用该带
全部凝结加热反推的上升约 27 mm/s，当前 Q 为其 3 倍。

**受控对照**：把上升支（声明 Q 向上）内大尺度凝结的潜热按 Task 2b 同一弱温度梯度
依据（Sobel, Nilsson & Polvani 2001）记为外输，地形凝结与下沉支不变。结果
（`a5-task3-wtg-large-scale-five-tables.log`）：雨峰降至 12.3 mm/day（第 10 轮），
dT 转为 −0.84 K，Q 降到 24.1 mm/s，与弱温度梯度估计一致；中纬 35–55° 低层纬向风
在第 9 轮首次转为西风（第 10 轮 +0.50 m/s，上层 +13.2）。但 10 轮残差停在
0.044–0.058 不再下降，末轮全球 E 1.47、P 1.72 mm/day，水量闭合相对差 0.143 超过
0.05 的门，报告正确拒绝；信风带 Q 归零、科氏驱动减半（v −1.41 → −0.44 m/s），
低层纬向风仍 −1.96 m/s。因此该记账不是候选：它消除了反馈，也抽走了驱动。
实验源码已保存为 `target/probe/a5-task3-wtg-large-scale-export.patch`，生产路径
恢复冻结的 Task 2b 记账。

**结构性结论**：两种记账向相反方向失败，成因相同——翻转质量通量 Q 由带均辐散
诊断，与凝结加热没有联系。留在本层则热压驱动的上升不受静力稳定度约束（Q 三倍于
弱温度梯度值），外输则加热对环流完全无作用。地球上两者由 `w = Q_heat/σ` 绑定
（弱温度梯度；Gill 1980 Eq. (2)、BSH99 Eq. (2) 中加热直接作为斜压模的质量源汇）。
把上升支质量交换与凝结加热绑定属于 Task 2 冻结闭合的设计修订，超出代理自行
验证的技术假设范围，见计划中的裁定项；在此之前不再调涡动幅度、层间交换或阻尼。

### 7.28 修订：补齐经向动量量测，校正潜热诊断边界（2026-09-08）

本轮从研究分支 `9937e6b` 续查，未改变物理算子、参数或 Q 闭合。
复用 §7.26 的生产算子，分别投影两层加速度到向极方向；纬带、面积权重与月份
采样保持原样。机械压力包含各层厚度压力和共同表面压力，配对项包含海面与层间
机械交换，供体项使用生产 `apply_declared_overturning_momentum`。
仍取因果 Draft seed 42 首次 P4 求解第五轮十二个月均值；以下加速度单位为
`1e-6 m/s²`，风速为 m/s。

**中纬 35–55°**：低层向极风 −0.2901，热压 −553.744、机械压 +341.428、
科氏 +211.612，三项几乎配平；完整倾向 +1.089。上层向极风 +0.2496，
热压 +816.381、机械压 +326.536、科氏 −1124.754、水平输运 −15.813，
完整倾向 +1.538。上层两项压力均向极，不能把向极流全归于单一潜热项。
快照分解说明瞬时力的平衡，不单独证明改动某项就能得到费雷尔环流；
§7.26 的机制解释继续作为待验证解释。

**信风带 10–20°**：低层向极风 −1.4138，热压 −160.135、机械压 +54.295、
科氏 +74.075、供体 +13.790、配对 +6.519，完整倾向 −3.717。
上层向极风 +1.9488，热压 +236.686、机械压 +30.247、科氏 −268.689，
完整倾向 −0.188。各项之和与完整倾向的十二月平均残差分别为 −0.005364、
+0.001135、−0.007467、+0.008893（中纬低／上、信风低／上），未新增验收阈值。

**潜热归属更正**：`apply_phase_change_latent_heat` 执行时，降水数组只含低层
凝结；之后 `apply_upper_condensation_after_exchange` 加入上层凝结并只加热上层。
§7.27 探针读取最终总降水，却没有扣除上层份额，“低层潜热”对一般格点不成立。
本轮临时插桩直接记录上层生产算子的实际凝结量，低层净潜热从总降水扣除对流
外输、上层凝结和陆地蒸散计量；量测字段随插桩一起删除，不进入产品契约。

雨峰 7.5° S 的十二个月上层凝结均为零（探针输出精度内），故 §7.27 雨峰数字
不变：P 31.9643、对流 21.5130、地形 1.2282 mm/day，低层净潜热 247.33 W/m²、
2.8958 K/day，温差 +4.5073 K、Q 向上 77.7478 mm/s。统计错层没有解释该雨峰。
其他纬带确有上层凝结，例如同轮 month 0 的 7.5° N 为 0.126182 mm/day。

**WTG 证据边界**：核对 Sobel, Nilsson & Polvani (2001)，*JAS* 58, 3650–3665
（[作者公开稿](https://www.ldeo.columbia.edu/~sobel/Papers/sobel_nilsson_polvani.pdf)）。
WTG 涉及自由对流层热力平衡、垂直结构和静力稳定度，不能仅用全部凝结潜热除以
未定义的稳定度，就把结果当成这里两层交换 Q 的同垂直模参考。§7.27 的
“约 27 mm/s、三倍”没有记录模型对应的稳定度、加热垂直投影和净热量分项，
本修订将其降为未复核估计，不用作定值或更换闭合的证据。下一步须审计辐射先验
已承担的纬向平均输送、实际净非绝热加热与垂直模映射，再形成具体修订方案。
本轮不据此估计提出裁定请求，也没有恢复上升支大尺度凝结外输实验。

**复现材料**：`target/probe/a5-task3-resume-momentum-latent.patch` 应用于
`9937e6b` 后，以 `CARGO_TARGET_DIR=target/probe` 运行
`cargo test --release --test p4_zonal_profile_probe -- --ignored --nocapture`。
补丁移除旧参考求解、加入本轮诊断，量测后已从生产源码删除。日志为同名前缀
`.log`，汇总为 `a5-task3-resume-momentum-latent-means.json`。PowerShell 日志折行
先按下一个 `[` 记录起点拼接，再选首次求解 `cycle=4` 按带和层平均十二个月。
补丁 SHA256 `cd27babfa9cf0ec73cff976a174a19342f5e25effc489e876e17d553cc9ff095`；
日志 SHA256 `b00182c31be2b75351cf6c4e43b0bad93b374fc96bfb9833222034f227d198fb`；
汇总 SHA256 `e221045525ebccdb9498789a7fbd17e5a5a35cf12389034c0fd3f80063a391a0`。

### 7.29 修订：移除强制研发求解，保持研究态发布边界（2026-09-08）

原生成循环无条件执行动量、热带与指定格点诊断，并在第四轮额外做短步差分和
宏步加密参考；参考路径的错误经 `?` 进入生成结果。失败后的诊断还使用 `expect`
与固定格点访问，存在覆盖原错误的风险。删除这些调用及失去消费者的诊断方法，
保留实际生产压力、输运、层间交换、私有水量求根和科学错误。本轮临时字段也已
删除，不新增开关、公共 API 或 artifact 内容。

压力测试移除夹带的旧阻尼精确断言：其失败来自 §7.2 研究映射与旧一日阻尼冲突，
不是压力守恒失败；改从 `ClimateLayerLayout` 取层厚、从生产常量取 C1 耦合值。
没有为通过测试修改阻尼，也没有将未冻结候选改为发布决定。现有主动取消集成
测试在清理前也通过，因此不声称已复现取消崩溃；本轮恢复直接错误传播，移除了
与生成结果无关的可失败工作。

本轮只清理编排与诊断，不更新方程指纹，不宣布 Task 3 质量通过。
UI 检查使用研究分支构建，验证生成、字段展示与取消，不能代替最终质量验收。
